# 🌐 วิธีเปิดเว็บไซต์ iLovePhone Store

## ⚡ วิธีที่เร็วที่สุด (1 คำสั่ง)

```bash
cd /Users/chakkapong/Sites/ilovephone-store && ./open-website.sh
```

---

## 🎯 วิธีทั้งหมด (เลือกวิธีที่ชอบ)

### 1️⃣ ใช้ Script (แนะนำ)

```bash
./open-website.sh
```

Script นี้จะ:
- ✅ ตรวจสอบว่าเซิร์ฟเวอร์ทำงานหรือไม่
- ✅ เริ่มเซิร์ฟเวอร์ถ้ายังไม่ทำงาน
- ✅ เปิดเบราว์เซอร์อัตโนมัติ

---

### 2️⃣ เปิดจาก Terminal

**Safari:**
```bash
open -a Safari http://localhost:8000
```

**Chrome:**
```bash
open -a "Google Chrome" http://localhost:8000
```

**Firefox:**
```bash
open -a Firefox http://localhost:8000
```

**เบราว์เซอร์ Default:**
```bash
open http://localhost:8000
```

---

### 3️⃣ พิมพ์ใน Address Bar

1. เปิดเบราว์เซอร์
2. คลิกที่ address bar (แถบ URL)
3. พิมพ์:
   ```
   http://localhost:8000
   ```
4. กด Enter

---

### 4️⃣ สร้าง Bookmark

1. เปิด `http://localhost:8000` ในเบราว์เซอร์
2. กด `Cmd + D` (Mac) หรือ `Ctrl + D` (Windows)
3. บันทึก Bookmark ชื่อ "iLovePhone Store"

---

## 🚫 สิ่งที่ไม่ควรทำ

❌ **อย่าเปิดผ่าน file://**
```
file:///Users/chakkapong/Sites/ilovephone-store/index.html  ❌ ผิด!
```

✅ **ต้องเปิดผ่าน http://**
```
http://localhost:8000  ✅ ถูกต้อง!
```

---

## 🔧 ถ้าเว็บเปิดไม่ขึ้น

### ตรวจสอบเซิร์ฟเวอร์
```bash
./check-servers.sh
```

### Restart เซิร์ฟเวอร์
```bash
./restart-servers.sh
```

### เปิดเว็บใหม่
```bash
./open-website.sh
```

---

## 💡 สร้าง Alias (ทางลัด)

เพิ่มในไฟล์ `~/.zshrc`:

```bash
# iLovePhone Store
alias ilove="cd /Users/chakkapong/Sites/ilovephone-store && ./open-website.sh"
alias ilove-open="open http://localhost:8000"
```

จากนั้นรัน:
```bash
source ~/.zshrc
```

ใช้งาน:
```bash
ilove        # เปิดเว็บพร้อมเช็คเซิร์ฟเวอร์
ilove-open   # เปิดเว็บเลย
```

---

## 📋 URL ที่ใช้งานได้

| URL | คำอธิบาย |
|-----|----------|
| `http://localhost:8000` | ✅ ใช้ได้ (แนะนำ) |
| `http://127.0.0.1:8000` | ✅ ใช้ได้ |
| `http://localhost:8000/index.html` | ✅ ใช้ได้ |
| `file:///path/to/index.html` | ❌ ใช้ไม่ได้ (CORS Error) |

---

## 🆘 แก้ไข CORS Error

### อาการ:
```
Access to fetch at 'http://localhost:8000/index.html' from origin 'null' 
has been blocked by CORS policy
```

### สาเหตุ:
เปิดไฟล์ผ่าน `file://` protocol

### วิธีแก้:
**เปิดผ่าน http:// แทน:**
```bash
open http://localhost:8000
```

**หรือใช้ script:**
```bash
./open-website.sh
```

---

## ✅ Checklist

ก่อนเปิดเว็บไซต์:

- [ ] เซิร์ฟเวอร์ทำงานอยู่ (`./check-servers.sh`)
- [ ] ใช้ URL `http://localhost:8000` (ไม่ใช่ file://)
- [ ] เปิดในเบราว์เซอร์ (Safari, Chrome, Firefox)
- [ ] Clear cache ถ้าจำเป็น (`Cmd + Shift + R`)

---

**📅 Last Updated:** 19 พ.ย. 2568

