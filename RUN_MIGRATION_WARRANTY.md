# 🚀 การรัน Migration สำหรับ Warranty (รับประกัน) ของรายการซ่อม

## 📋 คำอธิบาย

Migration นี้จะเพิ่มฟิลด์ `warranty` ลงในตาราง `repairs` เพื่อเก็บระยะเวลารับประกัน (เป็นจำนวนวัน)

## 🔧 วิธีรัน Migration

### 1. เริ่มต้น MySQL Server (ถ้ายังไม่ได้เปิด)
```bash
mysql.server start
```

### 2. รัน Migration Script
```bash
cd /Users/chakkapong/Sites/ilovephone-store
mysql -u root ilovephone_db < server/migrations/add_repair_warranty.sql
```

### 3. ตรวจสอบว่า Migration สำเร็จ
```bash
mysql -u root ilovephone_db -e "DESCRIBE repairs;"
```

คุณควรจะเห็นฟิลด์ `warranty` ในผลลัพธ์

## 📝 ฟิลด์ที่เพิ่มเข้ามา

- **`warranty`**: INT
  - ใช้เก็บระยะเวลารับประกันเป็นจำนวนวัน
  - ค่าที่เป็นไปได้: `7` (7 วัน), `30` (1 เดือน), `90` (3 เดือน), `180` (6 เดือน), `365` (1 ปี)
  - มี INDEX สำหรับการค้นหาที่เร็วขึ้น

## ⚠️ หมายเหตุ

- Migration นี้**จำเป็นต้องรัน**เพื่อให้ระบบทำงานได้ถูกต้อง
- ข้อมูลเก่าจะมี `warranty = NULL` (สามารถแก้ไขได้ภายหลัง)
- ข้อมูลใหม่ที่บันทึกหลังจากนี้จะต้องเลือกรับประกัน

## 🎯 ผลลัพธ์หลังรัน Migration

✅ ระบบจะสามารถบันทึกระยะเวลารับประกันได้  
✅ ข้อมูลใหม่จะถูกจัดเก็บอย่างเป็นระเบียบ  
✅ การค้นหาและแสดงผลจะเร็วขึ้น  

