# Claim Accessories Feature - ระบบเคลมพร้อมจัดการอะไหล่

## 📋 สรุปการเปลี่ยนแปลง

### 1. เมื่อกดเคลม (Claim)
- ✅ **คืนอะไหล่กลับสต็อคอัตโนมัติ**
- ระบบจะอ่านข้อมูลอะไหล่ที่ใช้จาก `accessories_used` field
- เพิ่ม `quantity` กลับไปในตาราง accessories (+1 ต่ออะไหล่)
- แสดงข้อความยืนยัน "บันทึกการเคลมสำเร็จ และคืนอะไหล่กลับสต็อคแล้ว"

### 2. เมื่อรับเครื่องจากเคลม (Receive from Claim)
- ✅ **เพิ่มระบบเลือกอะไหล่** เหมือนในแถบซ่อมเสร็จ
- สามารถเลือกอะไหล่หลายรายการได้
- แสดงรวมราคาทุนอะไหล่
- เพิ่มราคาทุนเพิ่มเติม (Manual Cost) ได้
- **ลดสต็อกอะไหล่**ที่เลือกทันที
- **อัพเดทการ์ดรายจ่าย** = อะไหล่ + ราคาทุนเพิ่มเติม
- **วันที่ซ่อมเสร็จเดิม (completed_date)** ไม่เปลี่ยนแปลง (สำหรับรับประกัน)
- **วันรับเครื่องใหม่ (returned_date)** อัพเดทเป็นวันที่รับจริง

## 🗂️ ไฟล์ที่แก้ไข

### 1. script.js

#### เพิ่มตัวแปร Global
```javascript
// Claim accessories management
let claimAccessoriesData = []; // Store accessories for claim modal
let claimAccessories = []; // Store selected accessories for claim modal
```

#### แก้ไข saveCompleteRepair
- เพิ่มการเก็บ `accessories_used` เป็น JSON
```javascript
const accessoriesJson = selectedAccessories.length > 0 ? JSON.stringify(selectedAccessories) : null;
accessories_used: accessoriesJson, // เก็บข้อมูลอะไหล่ที่ใช้
```

#### แก้ไข saveRepairClaim
- เพิ่มการคืนอะไหล่กลับสต็อค
```javascript
if (repair.accessories_used) {
    const usedAccessories = JSON.parse(repair.accessories_used);
    for (const usedAcc of usedAccessories) {
        // คืนสต็อก +1
        const newQuantity = currentQuantity + 1;
        await API.put(...)
    }
}
```

#### เพิ่มฟังก์ชันใหม่
1. `updateClaimAccessoryInfo()` - โหลดอะไหล่ตามประเภท
2. `filterClaimAccessories()` - ค้นหาอะไหล่
3. `addClaimAccessory()` - เพิ่มอะไหล่ลง list
4. `removeClaimAccessory(index)` - ลบอะไหล่จาก list
5. `renderClaimAccessories()` - แสดงรายการอะไหล่ที่เลือก
6. `calculateClaimTotalAccessoryCost()` - คำนวณราคารวม

#### แก้ไข openClaimToReceivedModal
- Clear accessories selection
- Render empty accessories list

#### แก้ไข saveClaimToReceived
- ลดสต็อกอะไหล่ที่เลือก (เหมือน completeRepair)
- คำนวณ accessory_cost จาก claimAccessories + manualCost
- บันทึก accessories_used เป็น JSON
- เพิ่มรายการอะไหล่ลง note

### 2. index.html

#### claimToReceivedModal - เพิ่ม UI
```html
<!-- อะไหล่ที่ใช้ -->
<div id="claimAccessoriesList">...</div>
<div id="claimTotalCostDisplay">...</div>

<!-- Add Accessory Form -->
<select id="claimAccessoryType" onchange="updateClaimAccessoryInfo()">...</select>
<input id="claimAccessorySearch" onkeyup="filterClaimAccessories()">
<input id="claimAccessoryCost" readonly>
<button onclick="addClaimAccessory()">+ เพิ่มอะไหล่</button>
<input id="claimManualCost" placeholder="ถ้ามีค่าใช้จ่ายอื่น">
```

### 3. server/migrations/add_accessories_used_to_repairs.sql
```sql
ALTER TABLE repairs 
ADD COLUMN IF NOT EXISTS accessories_used TEXT NULL AFTER note
COMMENT 'JSON array of accessories used in repair';
```

## 📊 Database Schema

### repairs table - เพิ่ม column
| Column | Type | Description |
|--------|------|-------------|
| accessories_used | TEXT | JSON array: `[{code, name, type, cost}]` |

## 🔄 Flow การทำงาน

### Flow 1: ซ่อมเสร็จ → เคลม
```
1. [ซ่อมเสร็จ] เลือกอะไหล่ → บันทึก accessories_used
2. ลดสต็อกอะไหล่ (quantity - 1)
3. กดเคลม → อ่าน accessories_used
4. คืนสต็อกอะไหล่ (quantity + 1)
5. ย้ายไปแถบเคลม
```

### Flow 2: เคลม → รับเครื่อง
```
1. [เคลม] กดรับเครื่อง
2. เลือกอะไหล่เพิ่มเติม (ถ้ามี)
3. กรอกค่าใช้จ่ายเพิ่มเติม (ถ้ามี)
4. ลดสต็อกอะไหล่ที่เลือก (quantity - 1)
5. อัพเดท accessory_cost = อะไหล่ + ค่าใช้จ่ายเพิ่มเติม
6. บันทึก accessories_used ใหม่
7. ย้ายไปแถบรับแล้ว
```

## ✅ การใช้งาน

### 1. รันสคริปต์ Migration
```bash
mysql -u root -p ilovephone_db < server/migrations/add_accessories_used_to_repairs.sql
```

### 2. ทดสอบฟีเจอร์

#### Test Case 1: เคลมและคืนอะไหล่
1. ซ่อมเสร็จและเลือกอะไหล่ (เช่น แบตเตอรี่)
2. ดูสต็อกอะไหล่ (quantity ลดลง)
3. กดเคลม
4. ตรวจสอบสต็อกอะไหล่ (quantity กลับมาเท่าเดิม) ✓

#### Test Case 2: รับเครื่องเคลมพร้อมอะไหล่ใหม่
1. จากแถบเคลม กดรับเครื่อง
2. เลือกอะไหล่เพิ่มเติม (เช่น จอ)
3. ใส่ราคาทุนเพิ่มเติม 100 บาท
4. บันทึก
5. ตรวจสอบการ์ดรายจ่าย (accessory_cost = ราคาจอ + 100) ✓
6. ตรวจสอบสต็อกจอ (quantity ลดลง) ✓
7. ตรวจสอบ completed_date (ยังคงเป็นวันเดิม) ✓
8. ตรวจสอบ returned_date (เป็นวันที่รับใหม่) ✓

## 🔍 Validation

### 1. การคืนอะไหล่
- ✅ อ่าน accessories_used จาก JSON
- ✅ เพิ่ม quantity +1 ต่ออะไหล่
- ✅ Handle error ถ้าไม่พบอะไหล่

### 2. การเลือกอะไหล่ใหม่
- ✅ แสดงเฉพาะอะไหล่ที่มีสต็อก (available > 0)
- ✅ ไม่ให้เลือกอะไหล่ซ้ำ
- ✅ คำนวณราคารวมถูกต้อง

### 3. การบันทึกข้อมูล
- ✅ บันทึก accessories_used เป็น JSON
- ✅ อัพเดท accessory_cost ถูกต้อง
- ✅ ไม่เปลี่ยน completed_date
- ✅ อัพเดท returned_date เป็นวันใหม่

## 📝 หมายเหตุ

1. **การคืนสต็อกอัตโนมัติ**: เมื่อกดเคลม ระบบจะคืนอะไหล่กลับสต็อคทันที ไม่ต้องทำเอง
2. **ค่าใช้จ่ายเพิ่มเติม**: ถ้ามีค่าใช้จ่ายอื่นๆ (ไม่ใช่อะไหล่) สามารถใส่ใน "ราคาทุนเพิ่มเติม" ได้
3. **วันรับประกัน**: ระบบจะเก็บ completed_date เดิมไว้ เพื่อใช้คำนวณรับประกัน
4. **การ์ดรายจ่าย**: รายการเคลมยังคงนับเป็นรายจ่ายตามปกติ (status: claimed)

## 🎯 ประโยชน์

1. ✅ จัดการสต็อกอะไหล่อัตโนมัติ
2. ✅ บันทึกค่าใช้จ่ายถูกต้องแม่นยำ
3. ✅ ไม่ต้องจำอะไหล่ที่ใช้ไป
4. ✅ รองรับการเคลมหลายรอบ
5. ✅ ข้อมูลรายจ่ายสมบูรณ์

---

**สร้างเมื่อ**: 11 ธันวาคม 2568
**เวอร์ชัน**: 1.0
**ผู้พัฒนา**: AI Assistant
