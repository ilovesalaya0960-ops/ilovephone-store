# อัปเดตข้อมูล package_sale สำหรับซิมที่ขายแล้ว

## 🎯 วัตถุประสงค์
อัปเดต `package_sale = 100` สำหรับเบอร์ **0989934742** ที่ขายแล้ว

## 📊 ข้อมูลปัจจุบัน (จากภาพ)
```
id: SIM-1764177137405
provider: DTAC
phone_number: 0989934742
package: เติมเงิน
package_sale: NULL          ← ต้องอัปเดตเป็น 100
status: sold
sale_date: 2025-11-26
```

---

## ⚡ วิธีอัปเดตข้อมูล

### วิธีที่ 1: ใช้ SQL Script (แนะนำ)

```bash
mysql -u root -p ilovephone_db < /Users/chakkapong/Sites/ilovephone-store/update-simcard-package-sale.sql
```

---

### วิธีที่ 2: รันคำสั่ง SQL โดยตรง

```bash
mysql -u root -p ilovephone_db
```

แล้วรัน:

```sql
-- อัปเดตข้อมูล
UPDATE simcards 
SET package_sale = 100
WHERE phone_number = '0989934742' 
  AND status = 'sold';

-- ตรวจสอบผลลัพธ์
SELECT 
    provider,
    phone_number,
    package,
    package_sale,
    status
FROM simcards 
WHERE phone_number = '0989934742';
```

**ผลลัพธ์ที่ต้องการ:**
```
+----------+------------+----------+--------------+------+
| provider | phone      | package  | package_sale | stat |
+----------+------------+----------+--------------+------+
| DTAC     | 0989934742 | เติมเงิน |       100.00 | sold |
+----------+------------+----------+--------------+------+
                                    ↑ ต้องเป็น 100.00
```

---

## 🔄 หลังอัปเดตข้อมูล

### 1. Hard Refresh Browser
- Mac: `Cmd + Shift + R`
- Windows: `Ctrl + Shift + F5`

### 2. ตรวจสอบในหน้าเว็บ

ไปที่แท็บ **"ขายแล้ว"** ควรเห็น:

| ผู้ให้บริการ | เบอร์โทร | รายการ | **ยอดเติม** | ราคาทุน | ราคาขาย |
|---------|---------|--------|----------|---------|---------|
| DTAC | 0989934742 | เติมเงิน (฿100) | **฿100** ✅ | ฿35.00 | ฿50.00 |

---

## 🧪 ทดสอบการขายซิมใหม่

เพื่อให้แน่ใจว่าระบบทำงานถูกต้อง:

1. เลือกซิมอื่น → **ขายแล้ว**
2. กรอก:
   - ประเภท: **เติมเงิน**
   - ยอดเติม: **100**
   - วันที่ขาย: วันนี้
3. กด **อัปซิมการ์ขาย**
4. ตรวจสอบว่า `package_sale` ถูกบันทึกอัตโนมัติ

```sql
-- ตรวจสอบซิมที่เพิ่งขาย
SELECT 
    phone_number,
    package,
    package_sale,
    sale_date
FROM simcards 
WHERE status = 'sold'
ORDER BY sale_date DESC
LIMIT 3;
```

---

## 📋 Checklist

- [ ] รัน SQL UPDATE อัปเดต package_sale = 100
- [ ] ตรวจสอบว่าอัปเดตสำเร็จ (SELECT)
- [ ] Hard refresh browser
- [ ] ดูว่าหน้าเว็บแสดง "฿100" แล้ว
- [ ] ทดสอบขายซิมใหม่
- [ ] ยืนยันว่าข้อมูลใหม่บันทึกถูกต้อง

---

## ⚠️ หมายเหตุ

### ซิมที่ขายไปก่อนหน้านี้
ซิมที่ขายไปก่อนที่จะมี column `package_sale` จะมีค่าเป็น `NULL` 
- ถ้าต้องการอัปเดตให้รัน UPDATE เหมือนด้านบน
- หรือปล่อยไว้เป็น NULL (ในตารางจะแสดง "-")

### ซิมที่จะขายต่อไป
ระบบจะบันทึก `package_sale` อัตโนมัติเมื่อ:
1. มี column `package_sale` ใน database ✅
2. Server route รองรับ `package_sale` ✅
3. กรอกยอดเติมใน Modal ขาย ✅

---

## 🔍 การตรวจสอบเพิ่มเติม

### ตรวจสอบว่าซิมอื่นมี package_sale หรือไม่
```sql
SELECT 
    COUNT(*) AS total_sold,
    COUNT(package_sale) AS has_package_sale,
    COUNT(*) - COUNT(package_sale) AS missing_package_sale
FROM simcards 
WHERE status = 'sold';
```

### ซิมที่ขายแล้วแต่ไม่มี package_sale
```sql
SELECT 
    phone_number,
    package,
    package_sale,
    sale_date
FROM simcards 
WHERE status = 'sold'
  AND package_sale IS NULL
ORDER BY sale_date DESC;
```

---

**สรุป**: รันคำสั่ง UPDATE ด้านบนแล้ว Refresh browser ครับ! 🚀

