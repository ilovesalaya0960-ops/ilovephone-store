# ♻️ คู่มือติดตั้งระบบเครื่องมือสอง

## 🎯 ภาพรวม

ระบบ "เครื่องมือสอง" ได้ถูกพัฒนาให้มีฟังก์ชันการทำงานเหมือนกับระบบ "จัดการเครื่องใหม่" ทุกประการ

### เปรียบเทียบ: เครื่องใหม่ vs เครื่องมือสอง

| ฟีเจอร์ | เครื่องใหม่ | เครื่องมือสอง | สถานะ |
|---------|-----------|-------------|-------|
| **UI/UX** |  |  |  |
| Dashboard Cards | ✅ | ✅ | เหมือนกัน |
| Tabs (Stock/Sold/Removed) | ✅ | ✅ | เหมือนกัน |
| Filter เดือน/ปี | ✅ | ✅ | เหมือนกัน |
| Search | ✅ | ✅ | เหมือนกัน |
| Modal Form | ✅ | ✅ | เหมือนกัน |
| **ฟังก์ชัน** |  |  |  |
| CREATE | ✅ | ✅ | เหมือนกัน |
| READ | ✅ | ✅ | เหมือนกัน |
| UPDATE | ✅ | ✅ | เหมือนกัน |
| DELETE | ✅ | ✅ | เหมือนกัน |
| **Database** |  |  |  |
| ตาราง | new_devices | used_devices | แยกตาราง |
| ฟิลด์พื้นฐาน | 17 ฟิลด์ | 18 ฟิลด์ | +1 (device_condition) |
| purchased_from | ✅ | ✅ | เหมือนกัน |
| device_category | ✅ | ✅ | เหมือนกัน |
| device_condition | ❌ | ✅ | เฉพาะมือสอง |

## 📋 โครงสร้างระบบ

### 1. Frontend (HTML/JavaScript)

#### Modal Form
- **ตำแหน่ง:** `index.html` บรรทัด 2307-2415
- **ID:** `usedDeviceModal`
- **ฟิลด์ทั้งหมด:**
  - ยี่ห้อ, รุ่น, สี, IMEI
  - RAM (1-16 GB)
  - ROM (32-1024 GB)
  - **ซื้อจาก** (purchased_from)
  - **หมวดเครื่อง** (device_category: No Active/Active)
  - **สภาพเครื่อง** (device_condition: excellent/good/fair/poor)
  - ราคารับซื้อ, ราคาขาย
  - วันที่รับซื้อ, วันที่ขาย
  - สถานะ, หมายเหตุ

#### Tabs Navigation
- **Stock:** แสดงเครื่องในสต๊อก
- **Sold:** แสดงเครื่องที่ขายแล้ว
- **Removed:** แสดงเครื่องที่ตัดออก

#### Filter & Search
- Filter เดือน (12 เดือน)
- Filter ปี (5 ปี)
- Search (ยี่ห้อ, รุ่น, IMEI, สี)

### 2. Backend API

#### Endpoints

```javascript
// GET all used devices
GET /api/usedDevices?store=salaya

// GET single used device
GET /api/usedDevices/:id

// POST create used device
POST /api/usedDevices

// PUT update used device
PUT /api/usedDevices/:id

// DELETE used device
DELETE /api/usedDevices/:id

// GET statistics
GET /api/usedDevices/stats/summary?store=salaya
```

#### Request Body Example (POST/PUT)

```json
{
  "id": "U1730374800000",
  "brand": "Samsung",
  "model": "Galaxy S21",
  "color": "Phantom Gray",
  "imei": "358240051111110",
  "ram": "8",
  "rom": "256",
  "purchased_from": "ลูกค้า นาย A",
  "device_category": "Active",
  "device_condition": "excellent",
  "purchase_price": 15000,
  "import_date": "2025-10-31",
  "sale_price": 19000,
  "sale_date": null,
  "status": "stock",
  "note": "",
  "store": "salaya"
}
```

### 3. Database Schema

#### ตาราง: `used_devices`

| ฟิลด์ | ประเภท | หมายเหตุ |
|-------|--------|----------|
| id | VARCHAR(50) | PRIMARY KEY |
| brand | VARCHAR(100) | ยี่ห้อ |
| model | VARCHAR(100) | รุ่น |
| color | VARCHAR(50) | สี |
| imei | VARCHAR(50) | IMEI (UNIQUE) |
| ram | VARCHAR(20) | RAM |
| rom | VARCHAR(20) | ROM |
| **purchased_from** | **VARCHAR(100)** | **ซื้อจาก (ใหม่)** |
| **device_category** | **ENUM('No Active', 'Active')** | **หมวดเครื่อง (ใหม่)** |
| **device_condition** | **VARCHAR(50)** | **สภาพเครื่อง (ใหม่)** |
| purchase_price | DECIMAL(10,2) | ราคารับซื้อ |
| sale_price | DECIMAL(10,2) | ราคาขาย |
| import_date | DATE | วันที่รับซื้อ |
| sale_date | DATE | วันที่ขาย |
| status | ENUM('stock', 'sold', 'removed') | สถานะ |
| note | TEXT | หมายเหตุ |
| store | VARCHAR(50) | ร้าน |
| created_at | TIMESTAMP | สร้างเมื่อ |
| updated_at | TIMESTAMP | แก้ไขล่าสุด |

**รวม:** 18 ฟิลด์

## 🚀 วิธีติดตั้ง

### ขั้นตอนที่ 1: อัพเดท Database

รันคำสั่งนี้เพื่ออัพเดทตาราง used_devices:

```bash
cd /Users/chakkapong/Sites/ilovephone-store
./update_used_devices.sh
```

หรือรัน SQL โดยตรง:

```bash
mysql -u root -p mobile_shop_db < server/migrations/update_used_devices_table.sql
```

**Migration จะเพิ่ม:**
- คอลัมน์ `purchased_from` (ซื้อจาก)
- คอลัมน์ `device_category` (หมวดเครื่อง)
- คอลัมน์ `device_condition` (สภาพเครื่อง)
- เปลี่ยนชื่อ `purchase_date` เป็น `import_date`

### ขั้นตอนที่ 2: Restart Server

```bash
cd server
npm start
```

### ขั้นตอนที่ 3: ทดสอบระบบ

#### วิธีที่ 1: ใช้เครื่องมือทดสอบอัตโนมัติ

```bash
open /Users/chakkapong/Sites/ilovephone-store/test-used-devices.html
```

คลิกปุ่ม **"🚀 เริ่มทดสอบทั้งหมด"**

#### วิธีที่ 2: ทดสอบในเว็บ

1. เปิดเว็บแอพพลิเคชัน
2. คลิกเมนู **"เครื่องมือสอง"**
3. ทดสอบ:
   - เพิ่มเครื่อง → กรอกข้อมูลทุกช่อง → บันทึก
   - แก้ไข → ตรวจสอบว่าแสดงข้อมูลถูกต้อง
   - Filter เดือน/ปี → กรองข้อมูล
   - Search → ค้นหา
   - ลบ → ลบข้อมูล

#### วิธีที่ 3: ตรวจสอบใน Sequel Ace

```sql
-- ตรวจสอบโครงสร้าง
DESCRIBE used_devices;

-- ดูข้อมูล
SELECT 
    brand, model, color,
    purchased_from,
    device_category,
    device_condition,
    purchase_price, sale_price,
    status
FROM used_devices
ORDER BY created_at DESC
LIMIT 5;
```

## ✅ Checklist ตรวจสอบ

### Database
- [ ] ตาราง `used_devices` มี 18 คอลัมน์
- [ ] คอลัมน์ `purchased_from` มีอยู่
- [ ] คอลัมน์ `device_category` มีอยู่
- [ ] คอลัมน์ `device_condition` มีอยู่
- [ ] คอลัมน์ `import_date` (เปลี่ยนจาก purchase_date)

### API Endpoints
- [ ] GET all devices ทำงานได้
- [ ] GET single device ทำงานได้
- [ ] POST create device ทำงานได้
- [ ] PUT update device ทำงานได้
- [ ] DELETE device ทำงานได้
- [ ] GET statistics ทำงานได้

### Frontend
- [ ] Modal form เปิดได้
- [ ] กรอกข้อมูลครบทุกฟิลด์
- [ ] ฟิลด์ "ซื้อจาก" แสดงและทำงาน
- [ ] ฟิลด์ "หมวดเครื่อง" แสดงและทำงาน
- [ ] ฟิลด์ "สภาพเครื่อง" แสดงและทำงาน
- [ ] บันทึกข้อมูลได้
- [ ] แก้ไขข้อมูลได้ (แสดงค่าเดิม)
- [ ] ลบข้อมูลได้
- [ ] Filter เดือน/ปี ทำงาน
- [ ] Search ทำงาน
- [ ] Tabs (Stock/Sold/Removed) ทำงาน

### CRUD Operations
- [ ] CREATE: สร้างเครื่องมือสองได้
- [ ] READ: ดึงข้อมูลแสดงในตารางได้
- [ ] UPDATE: แก้ไขข้อมูลได้
- [ ] DELETE: ลบข้อมูลได้

## 🔍 การทดสอบละเอียด

### Test Case 1: สร้างเครื่องมือสอง

```javascript
// Input
{
  brand: "iPhone",
  model: "13 Pro Max",
  color: "Sierra Blue",
  ram: "6",
  rom: "256",
  purchased_from: "ลูกค้า นางสาว B",
  device_category: "Active",
  device_condition: "good",
  purchase_price: 25000,
  sale_price: 32000,
  status: "stock"
}

// Expected Output
✅ บันทึกลง Database
✅ แสดงในตาราง Stock
✅ ฟิลด์ทั้งหมดครบถ้วน
```

### Test Case 2: แก้ไขข้อมูล

```javascript
// Action: คลิก "แก้ไข" ที่เครื่องที่สร้าง

// Expected Output
✅ Modal เปิด
✅ ฟิลด์ "ซื้อจาก" แสดงค่า "ลูกค้า นางสาว B"
✅ ฟิลด์ "หมวดเครื่อง" เลือก "Active"
✅ ฟิลด์ "สภาพเครื่อง" เลือก "good"
✅ แก้ไขและบันทึกได้
✅ ข้อมูลอัพเดทใน Database
```

### Test Case 3: Filter และ Search

```javascript
// Action 1: เลือกเดือน "ต.ค." และปี "2568"
// Expected: แสดงเฉพาะเครื่องในเดือน ต.ค. 2568

// Action 2: พิมพ์ "iPhone" ในช่องค้นหา
// Expected: แสดงเฉพาะเครื่อง iPhone

// Action 3: คลิก "ล้างตัวกรอง"
// Expected: แสดงทั้งหมด
```

## 📊 ความแตกต่างจากเครื่องใหม่

| ฟีเจอร์ | เครื่องใหม่ | เครื่องมือสอง |
|---------|-----------|-------------|
| ฟิลด์พื้นฐาน | เหมือนกัน | เหมือนกัน |
| purchased_from | ✅ | ✅ |
| device_category | ✅ | ✅ |
| **device_condition** | ❌ | ✅ (เฉพาะมือสอง) |
| ชื่อฟิลด์ราคา | purchase_price | purchase_price (รับซื้อ) |
| ชื่อฟิลด์วันที่ | import_date | import_date (รับซื้อ) |
| API Endpoint | /api/newDevices | /api/usedDevices |
| Database Table | new_devices | used_devices |

## 🐛 แก้ไขปัญหา

### ปัญหา 1: คอลัมน์ใหม่ไม่มี
**อาการ:** Error: Unknown column 'purchased_from'

**วิธีแก้:**
```bash
./update_used_devices.sh
```

### ปัญหา 2: ข้อมูลไม่แสดงเวลาแก้ไข
**อาการ:** ช่อง "ซื้อจาก" และ "หมวดเครื่อง" ว่าง

**วิธีแก้:**
- Hard refresh (Ctrl+Shift+R)
- เช็คว่า migration รันสำเร็จ
- Restart Server

### ปัญหา 3: API ไม่ทำงาน
**อาการ:** ไม่สามารถดึงข้อมูลได้

**วิธีแก้:**
```bash
cd server
npm start
```

## 📁 ไฟล์ที่เกี่ยวข้อง

| ไฟล์ | รายละเอียด |
|------|-----------|
| `server/routes/usedDevices.js` | API Routes |
| `server/migrations/update_used_devices_table.sql` | Database Migration |
| `update_used_devices.sh` | Script อัพเดท |
| `test-used-devices.html` | เครื่องมือทดสอบ |
| `USED_DEVICES_SETUP.md` | เอกสารนี้ |

## 🎉 สรุป

✅ **ระบบเครื่องมือสองมีฟังก์ชันครบเหมือนเครื่องใหม่ทุกประการ**

✅ **UI/UX เหมือนกัน 100%**

✅ **Database Structure สมบูรณ์**

✅ **API Endpoints ครบถ้วน**

✅ **CRUD Operations ทำงานได้**

✅ **Filter, Search, Tabs ทำงานได้**

✅ **พร้อมใช้งานจริง!**

---

**เวอร์ชั่น:** 1.0  
**อัพเดท:** 31 ตุลาคม 2025  
**ระบบ:** iLovePhone Store Management System

