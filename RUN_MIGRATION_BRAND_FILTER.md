# 🚀 การรัน Migration สำหรับ Brand Filter ของฟิล์มกันรอย

## 📋 คำอธิบาย

Migration นี้จะเพิ่มฟิลด์ `brand_filter` ลงในตาราง `equipment` เพื่อเก็บข้อมูลยี่ห้อเครื่องที่ฟิล์มกันรอยรองรับ

## 🔧 วิธีรัน Migration

### 1. เริ่มต้น MySQL Server (ถ้ายังไม่ได้เปิด)
```bash
mysql.server start
```

### 2. รัน Migration Script
```bash
cd /Users/chakkapong/Sites/ilovephone-store
mysql -u root ilovephone_db < server/migrations/add_equipment_brand_filter.sql
```

### 3. ตรวจสอบว่า Migration สำเร็จ
```bash
mysql -u root ilovephone_db -e "DESCRIBE equipment;"
```

คุณควรจะเห็นฟิลด์ `brand_filter` ในผลลัพธ์

## 📝 ฟิลด์ที่เพิ่มเข้ามา

- **`brand_filter`**: VARCHAR(50)
  - ใช้เก็บยี่ห้อเครื่องที่ฟิล์มรองรับ
  - ค่าที่เป็นไปได้: `apple`, `samsung`, `oppo`, `vivo`, `redmi`, `other`
  - มี INDEX สำหรับการค้นหาที่เร็วขึ้น

## ⚠️ หมายเหตุ

- Migration นี้**ไม่จำเป็นต้องรัน**ถ้าระบบยังใช้งานได้ปกติ
- ข้อมูลเก่าจะยังคงทำงานได้ผ่านระบบ fallback ที่ค้นหาจาก brand/model/code/note
- แต่ข้อมูลใหม่ที่บันทึกหลังจากนี้จะมีความแม่นยำและเร็วกว่า

## 🎯 ผลลัพธ์หลังรัน Migration

✅ ระบบจะสามารถกรองฟิล์มกันรอยตามยี่ห้อได้แม่นยำยิ่งขึ้น  
✅ ข้อมูลใหม่จะถูกจัดเก็บอย่างเป็นระเบียบ  
✅ การค้นหาและแสดงผลจะเร็วขึ้น  

