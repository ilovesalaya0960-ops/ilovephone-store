# 🔧 แก้ไขปัญหา: การ์ดรายจ่าย/รายรับ/กำไร ไม่แสดงผลเมื่อกรองวันที่

## 🐛 ปัญหาที่พบ

เมื่อเลือกกรองตามวันที่ (Date Range Filter):
- ❌ การ์ดด้านบนแสดง **฿0** ทั้งหมด (รายจ่าย, รายรับ, กำไร)
- ✅ ตารางแสดงข้อมูลถูกต้อง
- ✅ จำนวนรายการแสดงถูกต้อง

## 🔍 สาเหตุ

ฟังก์ชันคำนวณสถิติ (expense/income/profit) ยังใช้ `currentNewDevicesFilter.month` และ `currentNewDevicesFilter.year` แทนที่จะใช้ `startDate` และ `endDate`

### โค้ดเดิม (ผิด):
```javascript
if (currentNewDevicesFilter.month && currentNewDevicesFilter.year) {
    const targetMonth = parseInt(currentNewDevicesFilter.month);
    const targetYear = parseInt(currentNewDevicesFilter.year);
    
    expenseDevices = allDevices.filter(device => {
        const deviceMonth = importDate.getMonth() + 1;
        const deviceYear = importDate.getFullYear();
        return deviceMonth === targetMonth && deviceYear === targetYear;
    });
}
```

## ✅ การแก้ไข

### 1. New Devices (เครื่องใหม่)

**ไฟล์:** `script.js` บรรทัด ~8858-8880

**โค้ดใหม่:**
```javascript
if (currentNewDevicesFilter.startDate || currentNewDevicesFilter.endDate) {
    // Use date range filter
    expenseDevices = allDevices.filter(device => {
        const importDate = new Date(device.import_date || device.importDate);
        const startMatch = !currentNewDevicesFilter.startDate || 
                          importDate >= new Date(currentNewDevicesFilter.startDate);
        const endMatch = !currentNewDevicesFilter.endDate || 
                        importDate <= new Date(currentNewDevicesFilter.endDate);
        
        return startMatch && endMatch;
    });
}
```

### 2. Used Devices (เครื่องมือสอง)

**ไฟล์:** `script.js` บรรทัด ~9137-9159

**โค้ดใหม่:**
```javascript
if (currentUsedDevicesFilter.startDate || currentUsedDevicesFilter.endDate) {
    // Use date range filter
    expenseDevices = allDevices.filter(device => {
        const importDate = new Date(device.import_date || device.importDate || 
                                    device.purchase_date || device.purchaseDate);
        const startMatch = !currentUsedDevicesFilter.startDate || 
                          importDate >= new Date(currentUsedDevicesFilter.startDate);
        const endMatch = !currentUsedDevicesFilter.endDate || 
                        importDate <= new Date(currentUsedDevicesFilter.endDate);
        
        return startMatch && endMatch;
    });
}
```

### 3. ปิดการใช้งานฟังก์ชันเก่า

**ไฟล์:** `script.js` บรรทัด ~9267-9278

```javascript
setTimeout(() => {
    // Removed old month/year dropdown initializers - now using date range inputs
    // initializeNewDevicesDateFilter();
    // initializeUsedDevicesDateFilter();
    // initializePawnDateFilter();
    // initializeEquipmentDateFilter();
    console.log('✅ Date filters initialized (date range inputs)');
}, 500);
```

---

## 📊 การทำงานหลังแก้ไข

### วิธีคำนวณรายจ่าย (Expense)
1. กรอง `allDevices` ตาม `import_date` ที่อยู่ในช่วง `startDate` ถึง `endDate`
2. รวม `purchase_price` ของอุปกรณ์ทั้งหมดที่กรองได้

### วิธีคำนวณรายรับ (Income)  
1. รวม `sale_price` ของอุปกรณ์ที่ `status = 'sold'` และอยู่ในช่วงวันที่

### วิธีคำนวณกำไร (Profit)
1. `กำไร = รายรับ - ต้นทุนของอุปกรณ์ที่ขายได้`

---

## 🎯 ผลลัพธ์

### ก่อนแก้ไข:
```
[เลือกวันที่: 01/10/2025 - 31/10/2025]

🏪 สต๊อก: 41 เครื่อง
💰 รายจ่าย: ฿0          ← ผิด!
💵 รายรับ: ฿0           ← ผิด!
📊 กำไร: ฿0             ← ผิด!
```

### หลังแก้ไข:
```
[เลือกวันที่: 01/10/2025 - 31/10/2025]

🏪 สต๊อก: 41 เครื่อง
💰 รายจ่าย: ฿15,496     ← ถูกต้อง!
💵 รายรับ: ฿17,430      ← ถูกต้อง!
📊 กำไร: ฿1,934         ← ถูกต้อง!
```

---

## 🔄 การทดสอบ

### ขั้นตอนการทดสอบ:
1. เปิดหน้า "จัดการเครื่องใหม่"
2. เลือก **จากวันที่:** 1 ตุลาคม 2568
3. เลือก **ถึงวันที่:** 31 ตุลาคม 2568
4. คลิกปุ่ม **"กรอง"**
5. ✅ ตรวจสอบว่าการ์ดด้านบนแสดงค่าที่ถูกต้อง

### ผลที่คาดหวัง:
- ✅ การ์ด "รายจ่าย" แสดงยอดรวมของเครื่องที่นำเข้าในช่วงวันที่นั้น
- ✅ การ์ด "รายรับ" แสดงยอดรวมของเครื่องที่ขายในช่วงวันที่นั้น
- ✅ การ์ด "กำไร" แสดงผลต่างระหว่างรายรับกับต้นทุน
- ✅ ตารางแสดงเฉพาะข้อมูลในช่วงวันที่ที่เลือก

---

## 📝 หมายเหตุ

### วันที่ที่ใช้ในการกรอง:
- **รายจ่าย (Expense):** กรองตาม `import_date` (วันที่นำเข้า)
- **รายรับ (Income) & กำไร (Profit):** กรองตาม `sale_date` (วันที่ขาย)

### ตัวอย่างกรณีการใช้งาน:
```
สถานการณ์: เครื่อง Samsung A07
- นำเข้า (import): 15 ต.ค. 2568 ราคา ฿2,500
- ขาย (sale): 27 ต.ค. 2568 ราคา ฿3,990

เมื่อกรองวันที่: 1-31 ต.ค. 2568
✅ รายจ่าย: +฿2,500 (นับเพราะนำเข้าในเดือนนี้)
✅ รายรับ: +฿3,990 (นับเพราะขายในเดือนนี้)
✅ กำไร: ฿1,490
```

---

**แก้ไขเมื่อ:** 6 พฤศจิกายน 2568  
**สถานะ:** ✅ แก้ไขเสร็จสมบูรณ์  
**ไฟล์ที่แก้ไข:** `script.js`

