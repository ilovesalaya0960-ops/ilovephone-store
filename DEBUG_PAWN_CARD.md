# วิธีการ Debug การ์ดขายฝากที่ไม่แสดงข้อมูล

## ปัญหา
จากรูปภาพ:
- **รูปที่ 1**: หน้าเครื่องจำนำ แสดง 1 รายการ (infinix hot60pro)
- **รูปที่ 2**: หน้าเครื่องจำนำ แสดง 3 รายการขายฝากอยู่ (oppo A3x, Apple ipad genn11, Apple 17 pm)
- **รูปที่ 3**: แดชบอร์ด แสดง "ไม่มีข้อมูลในเดือนนี้"

## วิธีตรวจสอบ

### ขั้นตอนที่ 1: รีเฟรชหน้าเว็บ
```
กด F5
```

### ขั้นตอนที่ 2: เปิด Console
```
กด F12 → แท็บ Console
```

### ขั้นตอนที่ 3: ไปที่แดชบอร์ด
1. คลิกเมนู **"แดชบอร์ด"**
2. เลือกร้าน **"ร้านไผ่ใหญ่ - คลองโยง"** (ตรงกับรูปที่ 3)
3. เลือกเดือน **ตุลาคม 2025** (หรือเดือนปัจจุบัน)

### ขั้นตอนที่ 4: คลิกที่การ์ดขายฝาก
คลิกที่การ์ด **"รายการขายฝาก"** (การ์ดที่มีไอคอน 🏦)

### ขั้นตอนที่ 5: ดู Console Log

#### ถ้าทำงานปกติ จะเห็น:
```javascript
==================== PAWN CARD DETAILS ====================
Current Store: klongyong
Pawn devices from API: 4 items
All pawn data: [
  { id: "PWN123", brand: "infinix", model: "hot60pro", status: "returned", ... },
  { id: "PWN124", brand: "oppo", model: "A3x", status: "active", ... },
  { id: "PWN125", brand: "Apple", model: "ipad genn11", status: "active", ... },
  { id: "PWN126", brand: "Apple", model: "17 pm", status: "active", ... }
]
Status breakdown: {
  active: 3,
  returned: 1,
  seized: 0
}
Filtered (active only): 3 items
Active pawn details: [
  { id: "PWN124", brand: "oppo", ... },
  { id: "PWN125", brand: "Apple", ... },
  { id: "PWN126", brand: "Apple", ... }
]
=========================================================
Card details (pawn): {
  totalItems: 3,
  currentStore: "klongyong",
  currentMonth: "2025-10"
}
```

→ ควรแสดงตาราง 3 รายการ ✅

#### ถ้ามีปัญหา จะเห็น:

**กรณีที่ 1: ร้านไม่ตรงกัน**
```javascript
==================== PAWN CARD DETAILS ====================
Current Store: salaya    // ← เลือกร้านศาลายา
Pawn devices from API: 4 items
All pawn data: [
  { id: "PWN123", store: "klongyong", ... },  // ← แต่ข้อมูลเป็นคลองโยง
  { id: "PWN124", store: "klongyong", ... },
  { id: "PWN125", store: "klongyong", ... },
  { id: "PWN126", store: "klongyong", ... }
]
Status breakdown: { active: 0, ... }  // ← กรองหมด
Filtered (active only): 0 items       // ← ไม่มีข้อมูล
=========================================================
```

**วิธีแก้:**
- ตรวจสอบว่าเลือกร้านถูกต้องหรือไม่
- ดูที่ `All pawn data` ว่าข้อมูลอยู่ร้านไหน
- เปลี่ยนร้านให้ตรงกับข้อมูล

**กรณีที่ 2: ข้อมูลทั้งหมด returned แล้ว**
```javascript
==================== PAWN CARD DETAILS ====================
Current Store: klongyong
Pawn devices from API: 4 items
All pawn data: [
  { id: "PWN123", status: "returned", ... },  // ← รับคืนแล้ว
  { id: "PWN124", status: "returned", ... },  // ← รับคืนแล้ว
  { id: "PWN125", status: "returned", ... },  // ← รับคืนแล้ว
  { id: "PWN126", status: "returned", ... }   // ← รับคืนแล้ว
]
Status breakdown: {
  active: 0,      // ← ไม่มี active
  returned: 4,    // ← รับคืนหมดแล้ว
  seized: 0
}
Filtered (active only): 0 items
=========================================================
```

**วิธีแก้:**
- นี่เป็นสถานะปกติ (รับเครื่องคืนหมดแล้ว)
- การ์ดแสดงเฉพาะเครื่องที่ยัง **active** (ยังไม่รับคืน)
- ถ้าต้องการดูทั้งหมด → ไปที่เมนู **"เครื่องจำนำ"**

**กรณีที่ 3: API ไม่ส่งข้อมูล**
```javascript
==================== PAWN CARD DETAILS ====================
Current Store: klongyong
Pawn devices from API: 0 items  // ← ไม่มีข้อมูลจาก API
All pawn data: []
Status breakdown: { active: 0, returned: 0, seized: 0 }
Filtered (active only): 0 items
=========================================================
```

**วิธีแก้:**
- ตรวจสอบว่า API server ทำงานหรือไม่
- ตรวจสอบว่ามีข้อมูลในฐานข้อมูลหรือไม่
- ตรวจสอบ Network tab ใน Console

## สรุปสาเหตุและวิธีแก้

### สาเหตุที่ 1: ร้านไม่ตรงกัน ⚠️
**อาการ:** มีข้อมูล แต่ร้านไม่ตรง

**วิธีแก้:**
1. ดูที่ `All pawn data` ในConsole
2. เช็ค field `store` ของแต่ละรายการ
3. เปลี่ยนร้านใน dropdown ให้ตรง

**ตัวอย่าง:**
```javascript
// ข้อมูลในระบบ:
store: "klongyong"

// แต่เลือกใน dropdown:
Current Store: "salaya"

// → ไม่แสดง (ร้านไม่ตรง)
```

### สาเหตุที่ 2: ข้อมูลทั้งหมด returned แล้ว ✅
**อาการ:** มีข้อมูล แต่ status ไม่ใช่ active

**นี่เป็นพฤติกรรมปกติ:**
- การ์ดขายฝากแสดงเฉพาะ `status = 'active'`
- ถ้ารับเครื่องคืนแล้ว จะไม่แสดงในการ์ด
- ไปดูที่เมนู "เครื่องจำนำ" → แท็บ "รับเครื่องคืน" แทน

**ตัวอย่าง:**
```javascript
Status breakdown: {
  active: 0,      // ← ไม่มีเครื่องขายฝากอยู่
  returned: 3,    // ← รับคืนไปแล้ว 3 รายการ
  seized: 0
}
// → แสดง "ไม่มีข้อมูล" (เป็นปกติ)
```

### สาเหตุที่ 3: ยังไม่มีข้อมูลจริงๆ ✅
**อาการ:** API ส่งข้อมูล 0 รายการ

**นี่ก็เป็นปกติ:**
- ยังไม่มีเครื่องขายฝาก
- หรือเป็นร้านใหม่ที่ยังไม่มีข้อมูล

### สาเหตุที่ 4: API เสีย ❌
**อาการ:** Console แสดง Error

**วิธีแก้:**
1. ตรวจสอบว่า Node.js server ทำงานหรือไม่
2. เปิด Network tab ใน Console
3. ดูว่ามี error 500 หรือ 404 หรือไม่

## วิธีตรวจสอบข้อมูลในฐานข้อมูล

### ใช้ MySQL
```sql
-- ดูข้อมูลทั้งหมด
SELECT 
    id,
    brand,
    model,
    status,
    store,
    receive_date,
    return_date
FROM pawn_devices
ORDER BY receive_date DESC;

-- ดูเฉพาะที่ active
SELECT 
    id,
    brand,
    model,
    store,
    status
FROM pawn_devices
WHERE status = 'active';

-- ดูตามร้าน
SELECT 
    store,
    status,
    COUNT(*) as count
FROM pawn_devices
GROUP BY store, status;
```

### ผลลัพธ์ที่ควรเห็น (ตามรูป):
```
+--------+--------+------------+----------+-------------+
| id     | brand  | model      | status   | store       |
+--------+--------+------------+----------+-------------+
| PWN124 | oppo   | A3x        | active   | klongyong   |
| PWN125 | Apple  | ipad gen11 | active   | klongyong   |
| PWN126 | Apple  | 17 pm      | active   | klongyong   |
| PWN123 | infinix| hot60pro   | returned | klongyong   |
+--------+--------+------------+----------+-------------+
```

→ ควรมี 3 รายการที่ active ✅

## แผนผังการทำงาน

```
คลิกการ์ดขายฝาก
    ↓
showCardDetails('pawn')
    ↓
getCardDetailsByType('pawn')
    ↓
API.get(pawn, { store: currentStore })
    ↓
ได้ข้อมูลทั้งหมด
    ↓
filter: status === 'active'
    ↓
    ├─ ถ้ามีข้อมูล → แสดงตาราง
    └─ ถ้าไม่มี → แสดง "ไม่มีข้อมูล"
```

## สรุป

**ขั้นตอนการ Debug:**
1. ✅ รีเฟรช (F5)
2. ✅ เปิด Console (F12)
3. ✅ ไปแดशบอร์ด
4. ✅ เลือกร้าน "คลองโยง"
5. ✅ คลิกการ์ดขายฝาก
6. ✅ ดู Console log ทั้งหมด
7. ✅ ส่ง screenshot มาให้ดู

**ข้อมูลที่ต้องส่งมา:**
- Screenshot Console log ทั้งหมด
- ร้านที่เลือก
- เดือนที่เลือก
- จำนวนรายการที่แสดงในเมนู "เครื่องจำนำ"

จะช่วยวิเคราะห์ปัญหาได้แม่นยำขึ้น!

