# ✅ ระบบเครื่องมือสอง - อัพเดทให้เหมือนเครื่องใหม่ครบทุกฟังก์ชัน

## 🎯 สิ่งที่ทำเสร็จ

### 1. ✅ HTML Form (index.html)
เพิ่มฟิลด์ใหม่ในฟอร์ม "เพิ่มเครื่องมือสอง":

- **ซื้อจาก** (purchased_from) - Text input, Optional
- **หมวดเครื่อง** (device_category) - Dropdown: No Active / Active, Required
- **RAM** - เพิ่ม 1 GB และ 3 GB (รวม 8 ตัวเลือก: 1, 2, 3, 4, 6, 8, 12, 16 GB)

**ตำแหน่งในฟอร์ม:**
```
ROM
↓
ซื้อจาก (ใหม่)
↓
หมวดเครื่อง (ใหม่)
↓
ราคารับซื้อ
```

### 2. ✅ Database Migration
สร้างไฟล์: `server/migrations/add_used_devices_new_fields.sql`

**การทำงาน:**
- เพิ่มคอลัมน์ `purchased_from VARCHAR(100)`
- เพิ่มคอลัมน์ `device_category ENUM('No Active', 'Active') DEFAULT 'No Active'`
- แก้ไขชื่อคอลัมน์ให้ตรงกับ API:
  - `store_id` → `store`
  - `condition_status` → `device_condition`
  - `purchase_date` → `import_date`

**วิธีรัน:**
```bash
mysql -u root -p mobile_shop_db < server/migrations/add_used_devices_new_fields.sql
```

### 3. ✅ API Routes (server/routes/usedDevices.js)

#### GET /api/usedDevices
- ดึงข้อมูลเครื่องมือสองทั้งหมด
- รองรับ filter ตาม store
- ✅ รองรับฟิลด์ใหม่

#### GET /api/usedDevices/:id
- ดึงข้อมูลเครื่องเดียว
- ✅ รองรับฟิลด์ใหม่

#### POST /api/usedDevices
- สร้างเครื่องมือสองใหม่
- ✅ รับฟิลด์ใหม่: purchased_from, device_category
- ✅ ค่าเริ่มต้น device_category = 'No Active'

#### PUT /api/usedDevices/:id
- แก้ไขข้อมูลเครื่อง
- ✅ รองรับการอัพเดตฟิลด์ใหม่
- ✅ ใช้ข้อมูลเดิมถ้าไม่ส่งมา

#### DELETE /api/usedDevices/:id
- ลบเครื่อง
- ✅ ทำงานปกติ

#### GET /api/usedDevices/stats/summary (ใหม่!)
- ดึงสถิติ: จำนวนทั้งหมด, stock, sold, removed
- คำนวณ: stock_value, total_revenue, total_profit
- ✅ รองรับ filter ตาม store

### 4. ✅ JavaScript Functions (script.js)

#### saveUsedDevice()
```javascript
// ส่งข้อมูลฟิลด์ใหม่
purchased_from: formData.get('purchasedFrom') || '',
device_category: formData.get('deviceCategory') || 'No Active',
device_condition: formData.get('condition'),
import_date: formData.get('purchaseDate'),
```

#### openUsedDeviceModal()
```javascript
// โหลดและแสดงฟิลด์ใหม่เวลาแก้ไข
document.getElementById('usedPurchasedFrom').value = device.purchased_from || '';
document.getElementById('usedDeviceCategory').value = device.device_category || 'No Active';
```

### 5. ✅ ฟีเจอร์ที่มีอยู่แล้ว (ทำงานครบ)

- ✅ Tabs: Stock / Sold / Removed
- ✅ Filter เดือน (12 เดือน)
- ✅ Filter ปี (5 ปี)
- ✅ Search/ค้นหา
- ✅ แสดงในตาราง
- ✅ แก้ไข (รองรับฟิลด์ใหม่แล้ว)
- ✅ ลบ
- ✅ สลับร้าน (ศาลายา/คลองโยง)
- ✅ Dashboard สถิติ

## 📋 โครงสร้าง Database

### ตาราง: used_devices

| ฟิลด์ | ประเภท | หมายเหตุ |
|-------|--------|----------|
| id | VARCHAR(50) | PRIMARY KEY |
| brand | VARCHAR(100) | ยี่ห้อ |
| model | VARCHAR(100) | รุ่น |
| color | VARCHAR(50) | สี |
| imei | VARCHAR(50) | IMEI (UNIQUE) |
| ram | VARCHAR(20) | RAM |
| rom | VARCHAR(20) | ROM |
| **purchased_from** | **VARCHAR(100)** | **ซื้อจาก (ใหม่)** |
| **device_category** | **ENUM** | **หมวดเครื่อง (ใหม่)** |
| device_condition | VARCHAR(50) | สภาพเครื่อง |
| purchase_price | DECIMAL(10,2) | ราคารับซื้อ |
| sale_price | DECIMAL(10,2) | ราคาขาย |
| import_date | DATE | วันที่รับซื้อ |
| sale_date | DATE | วันที่ขาย |
| status | ENUM | stock/sold/removed |
| note | TEXT | หมายเหตุ |
| store | VARCHAR(50) | ร้าน |
| created_at | TIMESTAMP | สร้างเมื่อ |
| updated_at | TIMESTAMP | แก้ไขล่าสุด |

**รวม:** 19 ฟิลด์ (รวมฟิลด์ใหม่ 2 ฟิลด์)

## 🚀 วิธีติดตั้งและใช้งาน

### ขั้นตอนที่ 1: รัน Migration

```bash
# วิธีที่ 1: ใช้ MySQL command line
mysql -u root -p mobile_shop_db < server/migrations/add_used_devices_new_fields.sql

# วิธีที่ 2: ใช้ Sequel Ace
# 1. เปิด Sequel Ace
# 2. เชื่อมต่อ database mobile_shop_db
# 3. คัดลอกเนื้อหาจากไฟล์ add_used_devices_new_fields.sql
# 4. วาง (Paste) ใน Query editor
# 5. กด Execute (Cmd+R)
```

### ขั้นตอนที่ 2: Restart Server

```bash
cd server
npm start
```

### ขั้นตอนที่ 3: ทดสอบ

1. เปิดเว็บ
2. คลิกเมนู "เครื่องมือสอง"
3. คลิก "+ เพิ่มเครื่องมือสอง"
4. จะเห็นฟิลด์ใหม่:
   - **ซื้อจาก** (หลัง ROM)
   - **หมวดเครื่อง** (หลัง ซื้อจาก)
   - **RAM** มีตัวเลือก 1 GB และ 3 GB
5. กรอกข้อมูลและบันทึก
6. ทดสอบแก้ไข - ช่องใหม่ต้องแสดงข้อมูล

## 🧪 ตรวจสอบใน Sequel Ace

### ตรวจสอบโครงสร้างตาราง

```sql
DESCRIBE used_devices;
```

ต้องเห็น:
```
purchased_from      varchar(100)
device_category     enum('No Active','Active')
```

### ทดสอบเพิ่มข้อมูล

```sql
INSERT INTO used_devices 
(id, brand, model, color, ram, rom, purchased_from, device_category, 
 device_condition, purchase_price, sale_price, import_date, status, store)
VALUES 
('U001', 'Apple', 'iPhone 12', 'White', '4', '128', 
 'ลูกค้า A', 'Active', 'excellent', 20000, 25000, CURDATE(), 'stock', 'salaya');
```

### ตรวจสอบข้อมูล

```sql
SELECT 
    brand,
    model,
    purchased_from,
    device_category,
    device_condition,
    purchase_price,
    status
FROM used_devices
ORDER BY created_at DESC
LIMIT 5;
```

## 📊 เปรียบเทียบกับเครื่องใหม่

| ฟีเจอร์ | เครื่องใหม่ | เครื่องมือสอง | สถานะ |
|---------|-------------|---------------|-------|
| **ฟิลด์พื้นฐาน** | | | |
| ยี่ห้อ, รุ่น, สี | ✅ | ✅ | เหมือนกัน |
| IMEI, RAM, ROM | ✅ | ✅ | เหมือนกัน |
| **ฟิลด์ใหม่** | | | |
| ซื้อจาก | ✅ | ✅ | เหมือนกัน |
| หมวดเครื่อง | ✅ | ✅ | เหมือนกัน |
| RAM 1/3 GB | ✅ | ✅ | เหมือนกัน |
| **ฟิลด์เฉพาะ** | | | |
| วันที่นำเข้า | ✅ | - | - |
| วันที่รับซื้อ | - | ✅ | - |
| สภาพเครื่อง | - | ✅ | เฉพาะมือสอง |
| **ฟังก์ชัน** | | | |
| CRUD | ✅ | ✅ | เหมือนกัน |
| Filter เดือน/ปี | ✅ | ✅ | เหมือนกัน |
| Search | ✅ | ✅ | เหมือนกัน |
| Tabs | ✅ | ✅ | เหมือนกัน |
| Dashboard | ✅ | ✅ | เหมือนกัน |

## ✅ Checklist การอัพเดต

### Frontend (HTML)
- [x] เพิ่มฟิลด์ "ซื้อจาก" (text input)
- [x] เพิ่มฟิลด์ "หมวดเครื่อง" (dropdown)
- [x] เพิ่ม RAM 1 GB
- [x] เพิ่ม RAM 3 GB

### Backend (API)
- [x] อัพเดท POST endpoint รับฟิลด์ใหม่
- [x] อัพเดท PUT endpoint อัพเดตฟิลด์ใหม่
- [x] อัพเดท GET endpoint ส่งฟิลด์ใหม่กลับ
- [x] เพิ่ม GET /stats/summary endpoint

### Database
- [x] สร้าง migration script
- [x] เพิ่มคอลัมน์ purchased_from
- [x] เพิ่มคอลัมน์ device_category
- [x] แก้ไขชื่อคอลัมน์ให้ตรงกัน

### JavaScript
- [x] อัพเดท saveUsedDevice() ส่งฟิลด์ใหม่
- [x] อัพเดท openUsedDeviceModal() แสดงฟิลด์ใหม่
- [x] อัพเดท field mapping (import_date, device_condition)

### ฟีเจอร์
- [x] ทำงานเหมือนเครื่องใหม่ทุกอย่าง
- [x] Filter เดือน/ปี
- [x] Search
- [x] CRUD complete
- [x] Dashboard สถิติ

## 🎯 สรุป

✅ **ระบบเครื่องมือสองอัพเดทเสร็จสมบูรณ์**

✅ **มีฟีเจอร์เหมือนเครื่องใหม่ทุกอย่าง**

✅ **ฟิลด์ใหม่: ซื้อจาก, หมวดเครื่อง, RAM 1/3 GB**

✅ **พร้อมใช้งานหลังรัน Migration และ Restart Server**

## 📝 หมายเหตุ

### ความแตกต่างระหว่างเครื่องใหม่และมือสอง

**เครื่องใหม่:**
- นำเข้าจากซัพพลายเออร์
- วันที่นำเข้า (import_date)
- ไม่มีสภาพเครื่อง (สภาพใหม่ทั้งหมด)

**เครื่องมือสอง:**
- รับซื้อจากลูกค้า
- วันที่รับซื้อ (import_date)
- มีสภาพเครื่อง (excellent/good/fair/poor)

### ฟิลด์ใหม่ที่เหมือนกัน
- ✅ ซื้อจาก (purchased_from)
- ✅ หมวดเครื่อง (device_category)
- ✅ RAM 1 GB และ 3 GB

---

**เวอร์ชั่น:** 2.0  
**วันที่:** 31 ตุลาคม 2025  
**สถานะ:** ✅ พร้อมใช้งาน

