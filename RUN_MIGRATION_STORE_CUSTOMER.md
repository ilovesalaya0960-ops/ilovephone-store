# 📋 คำแนะนำ: เพิ่มข้อมูลลูกค้าแบบละเอียดสำหรับผ่อนร้าน

## 📝 สรุปการเปลี่ยนแปลง

ระบบได้เพิ่มฟอร์มข้อมูลลูกค้าแบบละเอียดสำหรับ**รายการผ่อนร้าน**เท่านั้น ประกอบด้วย:

### 📱 หน้า UI
- ✅ เพิ่มตัวเลือกประเภทลูกค้า: หลนมือใหม่ (ไทย) / ชาวต่างชาติ
- ✅ รูปถ่ายประชาชน (upload)
- ✅ ข้อมูลลูกค้า: ชื่อ, โทรศัพท์, โทรศัพท์ที่ทำงาน, อีเมล
- ✅ ไอดีไลน์, เพศลูก, ค่าแนบประชาชน, บัตรประชาชน, วันเกิด
- ✅ บุคคลเกี่ยวข้อง 2 คน (ชื่อ-สกุล, ความสัมพันธ์, เบอร์โทร)
- ✅ ที่อยู่ตามบัตรประชาชน (บ้านเลขที่, จังหวัด, อำเภอ, ตำบล)
- ✅ ที่อยู่ปัจจุบัน (พร้อม checkbox "ที่อยู่เดียวกันกับประชาชน")
- ✅ ที่อยู่ทำงาน
- ✅ หมายเหตุ

### 💾 Database
เพิ่ม 34 คอลัมน์ใหม่ในตาราง `installment_devices`:

#### ข้อมูลลูกค้าพื้นฐาน (10 fields)
- `customer_type` - ประเภทลูกค้า (thai/foreigner)
- `customer_full_name` - ชื่อเต็ม
- `customer_phone_store` - เบอร์โทรศัพท์
- `customer_work_phone` - เบอร์โทรศัพท์ที่ทำงาน
- `customer_email` - อีเมล
- `customer_line_id` - ไอดีไลน์
- `customer_gender` - เพศลูก
- `customer_id_attach_fee` - ค่าแนบประชาชน
- `customer_id_number` - บัตรประชาชน (มี index)
- `customer_birthdate` - วันเกิด

#### บุคคลเกี่ยวข้อง (6 fields)
- `contact1_name`, `contact1_relation`, `contact1_phone`
- `contact2_name`, `contact2_relation`, `contact2_phone`

#### ที่อยู่ 3 แบบ (12 fields each = 12 fields total)
- **ที่อยู่บัตร**: `id_address_house_no`, `id_address_province`, `id_address_district`, `id_address_subdistrict`
- **ที่อยู่ปัจจุบัน**: `current_address_*`
- **ที่อยู่ทำงาน**: `work_address_*`

#### อื่นๆ (1 field)
- `customer_notes` - หมายเหตุ

---

## 🚀 วิธีรัน Migration

### ขั้นตอนที่ 1: เชื่อมต่อ MySQL

```bash
# เปิด Terminal และเชื่อมต่อ MySQL
mysql -u root -p
```

### ขั้นตอนที่ 2: เลือก Database

```sql
-- ใช้ database ของคุณ
USE ilovephone_db;
```

### ขั้นตอนที่ 3: รัน Migration

```sql
-- รันไฟล์ migration
SOURCE /Users/chakkapong/Sites/ilovephone-store/server/migrations/add_store_customer_details.sql;
```

### ขั้นตอนที่ 4: ตรวจสอบผลลัพธ์

```sql
-- ตรวจสอบว่าคอลัมน์ใหม่ถูกเพิ่มแล้ว
DESCRIBE installment_devices;

-- ตรวจสอบ indexes
SHOW INDEX FROM installment_devices;
```

---

## ✅ การทดสอบ

1. ✅ เปิดหน้าเครื่องผ่อน
2. ✅ คลิก "+ เพิ่มรายการผ่อน"
3. ✅ เลือก "รายการผ่อนร้าน"
4. ✅ ตรวจสอบว่าฟอร์มข้อมูลลูกค้าแบบละเอียดแสดงขึ้นมา
5. ✅ กรอกข้อมูลและบันทึก
6. ✅ ตรวจสอบว่าข้อมูลถูกบันทึกในฐานข้อมูลถูกต้อง

---

## 📊 ตัวอย่างการใช้งาน

### ดูข้อมูลลูกค้าที่บันทึกไว้

```sql
SELECT 
    id,
    customer_name,
    customer_full_name,
    customer_id_number,
    customer_phone_store,
    contact1_name,
    contact1_phone,
    id_address_province,
    installment_type
FROM installment_devices
WHERE installment_type = 'store'
ORDER BY created_at DESC
LIMIT 10;
```

### ค้นหาลูกค้าด้วยบัตรประชาชน

```sql
SELECT * FROM installment_devices
WHERE customer_id_number = 'เลขบัตร 13 หลัก';
```

---

## ⚠️ หมายเหตุ

1. ✅ ข้อมูลลูกค้าแบบละเอียดจะแสดงเฉพาะเมื่อเลือก **"รายการผ่อนร้าน"** เท่านั้น
2. ✅ สำหรับ **"รายการผ่อน partner"** จะใช้ฟอร์มแบบเดิม (ชื่อและเบอร์โทรเท่านั้น)
3. ✅ ฟิลด์ที่มี * (required) จะต้องกรอกข้อมูลก่อนบันทึก
4. ✅ รองรับการ upload รูปภาพบัตรประชาชน (เตรียมไว้สำหรับอนาคต)
5. ✅ มี checkbox "ที่อยู่เดียวกันกับประชาชน" เพื่อ copy ที่อยู่อัตโนมัติ

---

## 🔄 Rollback (ถ้าต้องการยกเลิก)

```sql
-- ลบคอลัมน์ทั้งหมดที่เพิ่มเข้าไป
ALTER TABLE installment_devices 
DROP COLUMN customer_type,
DROP COLUMN customer_full_name,
DROP COLUMN customer_phone_store,
DROP COLUMN customer_work_phone,
DROP COLUMN customer_email,
DROP COLUMN customer_line_id,
DROP COLUMN customer_gender,
DROP COLUMN customer_id_attach_fee,
DROP COLUMN customer_id_number,
DROP COLUMN customer_birthdate,
DROP COLUMN contact1_name,
DROP COLUMN contact1_relation,
DROP COLUMN contact1_phone,
DROP COLUMN contact2_name,
DROP COLUMN contact2_relation,
DROP COLUMN contact2_phone,
DROP COLUMN id_address_house_no,
DROP COLUMN id_address_province,
DROP COLUMN id_address_district,
DROP COLUMN id_address_subdistrict,
DROP COLUMN current_address_house_no,
DROP COLUMN current_address_province,
DROP COLUMN current_address_district,
DROP COLUMN current_address_subdistrict,
DROP COLUMN work_address_house_no,
DROP COLUMN work_address_province,
DROP COLUMN work_address_district,
DROP COLUMN work_address_subdistrict,
DROP COLUMN customer_notes;

-- ลบ indexes
DROP INDEX idx_customer_id_number ON installment_devices;
DROP INDEX idx_customer_phone_store ON installment_devices;
```

---

## 📞 ติดต่อ

หากมีปัญหาหรือข้อสงสัย กรุณาติดต่อทีมพัฒนา

---

**สร้างเมื่อ:** 17 มกราคม 2568  
**เวอร์ชัน:** 1.0.0

