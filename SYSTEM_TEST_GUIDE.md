# 📋 คู่มือตรวจสอบระบบจัดการเครื่องใหม่

## 🎯 วัตถุประสงค์

ตรวจสอบว่าระบบ "จัดการเครื่องใหม่" ทำงานครบถ้วนและบันทึกข้อมูลลง Database (MySQL ผ่าน Sequel Ace) ได้อย่างถูกต้อง

## 📦 สิ่งที่ระบบต้องมี

### 1. Frontend (HTML/JavaScript)
- ✅ ฟอร์มเพิ่มเครื่องใหม่ (Modal)
- ✅ ตารางแสดงข้อมูล (Stock/Sold/Removed)
- ✅ ฟังก์ชัน Filter (เดือน/ปี)
- ✅ ฟังก์ชัน Search
- ✅ ปุ่ม แก้ไข/ลบ/ดูรายละเอียด

### 2. Backend (Node.js + Express)
- ✅ API Endpoints:
  - `GET /api/newDevices` - ดึงข้อมูลทั้งหมด
  - `GET /api/newDevices/:id` - ดึงข้อมูลเครื่องเดียว
  - `POST /api/newDevices` - สร้างเครื่องใหม่
  - `PUT /api/newDevices/:id` - แก้ไขข้อมูล
  - `DELETE /api/newDevices/:id` - ลบข้อมูล
  - `GET /api/newDevices/stats/summary` - สถิติ

### 3. Database (MySQL)
- ✅ ตาราง `new_devices` พร้อมฟิลด์ทั้งหมด
- ✅ ฟิลด์พื้นฐาน: id, brand, model, color, imei, ram, rom
- ✅ ฟิลด์ใหม่: purchased_from, device_category
- ✅ ฟิลด์ราคา: purchase_price, sale_price
- ✅ ฟิลด์วันที่: import_date, sale_date
- ✅ ฟิลด์อื่นๆ: status, note, store, created_at, updated_at

## 🧪 วิธีการทดสอบ

### วิธีที่ 1: ใช้เครื่องมือทดสอบอัตโนมัติ (แนะนำ)

1. เปิดไฟล์ทดสอบในเบราว์เซอร์:
```bash
open /Users/chakkapong/Sites/ilovephone-store/test-complete-system.html
```

หรือ
```
http://localhost:8080/test-complete-system.html
```

2. คลิกปุ่ม "🚀 เริ่มทดสอบทั้งหมด"

3. รอจนการทดสอบเสร็จ (ประมาณ 10 วินาที)

4. ดูผลสรุป:
   - ✅ สีเขียว = ผ่านการทดสอบ
   - ❌ สีแดง = ไม่ผ่านการทดสอบ

#### การทดสอบที่จะรัน (7 ข้อ):

1. **ทดสอบการเชื่อมต่อ Database**
   - ตรวจสอบ API Server ทำงานหรือไม่
   - ตรวจสอบเชื่อมต่อ MySQL ได้หรือไม่

2. **ทดสอบดึงข้อมูลทั้งหมด (GET All)**
   - ดึงรายการเครื่องทั้งหมด
   - แสดงฟิลด์ทั้งหมด รวมฟิลด์ใหม่

3. **ทดสอบสร้างเครื่องใหม่ (CREATE)**
   - สร้างเครื่องทดสอบ
   - บันทึกฟิลด์ทั้งหมดรวมฟิลด์ใหม่
   - ตรวจสอบว่าบันทึกลง Database จริง

4. **ทดสอบดึงข้อมูลเครื่องเดียว (READ)**
   - ดึงข้อมูลเครื่องที่สร้างไว้
   - ตรวจสอบว่าฟิลด์ทั้งหมดครบถ้วน

5. **ทดสอบแก้ไขข้อมูล (UPDATE)**
   - แก้ไขข้อมูลเครื่องทดสอบ
   - แก้ไขฟิลด์ใหม่ (ซื้อจาก, หมวดเครื่อง)
   - ตรวจสอบว่าอัพเดทจริงใน Database

6. **ทดสอบลบข้อมูล (DELETE)**
   - ลบเครื่องทดสอบ
   - ตรวจสอบว่าถูกลบจาก Database จริง

7. **ทดสอบฟิลด์ทั้งหมด**
   - สร้างเครื่องพร้อมข้อมูลครบทุกฟิลด์
   - ตรวจสอบว่าฟิลด์ทั้งหมดบันทึกถูกต้อง
   - เช็คฟิลด์ใหม่โดยเฉพาะ

### วิธีที่ 2: ทดสอบด้วยตนเองในเว็บ

#### ขั้นตอนที่ 1: เตรียมระบบ
```bash
# 1. Start Server
cd server
npm start

# 2. เปิดเว็บ
open index.html
```

#### ขั้นตอนที่ 2: ทดสอบเพิ่มเครื่องใหม่

1. คลิกเมนู "จัดการเครื่องใหม่"
2. คลิกปุ่ม "+ เพิ่มเครื่องใหม่"
3. กรอกข้อมูล:
   ```
   ยี่ห้อ: Samsung
   รุ่น: Galaxy S23
   สี: ดำ
   IMEI: 123456789012345
   RAM: 8 GB
   ROM: 256 GB
   ซื้อจาก: MBK Center
   หมวดเครื่อง: Active
   ราคาซื้อ: 25000
   วันที่นำเข้า: (วันนี้)
   ราคาขาย: 30000
   สถานะ: สต็อค
   ```
4. คลิก "บันทึก"
5. ตรวจสอบว่าเครื่องปรากฏในตาราง

#### ขั้นตอนที่ 3: ตรวจสอบใน Sequel Ace

1. เปิด Sequel Ace
2. เชื่อมต่อฐานข้อมูล
3. เลือกฐานข้อมูล: `mobile_shop_db` หรือ `ilovephone_db`
4. เปิดตาราง `new_devices`
5. รัน Query:
```sql
SELECT 
    id,
    brand,
    model,
    color,
    ram,
    rom,
    purchased_from,
    device_category,
    purchase_price,
    sale_price,
    status,
    store
FROM new_devices
ORDER BY created_at DESC
LIMIT 5;
```

6. ตรวจสอบว่า:
   - ✅ มีข้อมูลที่เพิ่งสร้าง
   - ✅ ฟิลด์ `purchased_from` มีค่า "MBK Center"
   - ✅ ฟิลด์ `device_category` มีค่า "Active"
   - ✅ ข้อมูลอื่นๆ ถูกต้องครบถ้วน

#### ขั้นตอนที่ 4: ทดสอบแก้ไข

1. ในเว็บ คลิกปุ่ม "แก้ไข" ที่เครื่องที่เพิ่งสร้าง
2. Modal จะเปิดขึ้นพร้อมข้อมูลเดิม
3. เช็คว่า:
   - ✅ ช่อง "ซื้อจาก" แสดงค่า "MBK Center"
   - ✅ ช่อง "หมวดเครื่อง" เลือก "Active" อยู่
4. แก้ไขข้อมูล:
   ```
   ซื้อจาก: Pantip Plaza
   หมวดเครื่อง: No Active
   ราคาขาย: 31000
   ```
5. คลิก "บันทึก"
6. กลับไปดูใน Sequel Ace:
```sql
SELECT purchased_from, device_category, sale_price
FROM new_devices
WHERE brand = 'Samsung' AND model = 'Galaxy S23'
ORDER BY created_at DESC
LIMIT 1;
```

7. ตรวจสอบว่าข้อมูลอัพเดท

#### ขั้นตอนที่ 5: ทดสอบ Filter

1. เลือกเดือนจาก dropdown (เช่น "ต.ค.")
2. เลือกปี (เช่น "2568")
3. ตรวจสอบว่าตารางแสดงเฉพาะเครื่องในเดือน/ปีนั้น
4. คลิก "ล้างตัวกรอง" - ควรแสดงทั้งหมด

#### ขั้นตอนที่ 6: ทดสอบลบ

1. คลิกปุ่ม "ลบ" ที่เครื่องทดสอบ
2. ยืนยันการลบ
3. ตรวจสอบว่าเครื่องหายจากตาราง
4. เช็คใน Sequel Ace:
```sql
SELECT * FROM new_devices WHERE brand = 'Samsung' AND model = 'Galaxy S23';
```
5. ควรไม่มีข้อมูล (ถูกลบแล้ว)

## 📊 Checklist การทดสอบ

### CRUD Operations
- [ ] **CREATE**: สร้างเครื่องใหม่ได้
- [ ] **READ**: ดึงข้อมูลแสดงในตารางได้
- [ ] **UPDATE**: แก้ไขข้อมูลได้
- [ ] **DELETE**: ลบข้อมูลได้

### ฟิลด์พื้นฐาน
- [ ] ยี่ห้อ (brand)
- [ ] รุ่น (model)
- [ ] สี (color)
- [ ] IMEI
- [ ] RAM
- [ ] ROM

### ฟิลด์ใหม่ (สำคัญ!)
- [ ] ซื้อจาก (purchased_from)
- [ ] หมวดเครื่อง (device_category)

### ฟิลด์ราคา
- [ ] ราคาซื้อ (purchase_price)
- [ ] ราคาขาย (sale_price)

### ฟิลด์วันที่
- [ ] วันที่นำเข้า (import_date)
- [ ] วันที่ขาย (sale_date)

### ฟิลด์อื่นๆ
- [ ] สถานะ (status: stock/sold/removed)
- [ ] หมายเหตุ (note)
- [ ] ร้าน (store: salaya/klongyong)

### ฟังก์ชันเพิ่มเติม
- [ ] Filter เดือน
- [ ] Filter ปี
- [ ] Search/ค้นหา
- [ ] สลับร้าน (ศาลายา/คลองโยง)
- [ ] แสดงสถิติ (Dashboard)

## 🔍 ตรวจสอบใน Database โดยตรง

### ตรวจสอบโครงสร้างตาราง
```sql
DESCRIBE new_devices;
```

ควรเห็น:
```
purchased_from      varchar(100)
device_category     enum('No Active','Active')
```

### ตรวจสอบข้อมูล
```sql
-- ดูข้อมูลล่าสุด 10 รายการ
SELECT 
    brand,
    model,
    purchased_from,
    device_category,
    purchase_price,
    sale_price,
    status,
    created_at
FROM new_devices
ORDER BY created_at DESC
LIMIT 10;
```

### ตรวจสอบฟิลด์ใหม่มีข้อมูลหรือไม่
```sql
SELECT 
    COUNT(*) as total,
    COUNT(purchased_from) as has_purchased_from,
    COUNT(device_category) as has_device_category
FROM new_devices;
```

### ตรวจสอบ Device Category
```sql
SELECT 
    device_category,
    COUNT(*) as count
FROM new_devices
GROUP BY device_category;
```

## ⚠️ ปัญหาที่อาจพบ

### 1. Server ไม่ตอบสนอง
**อาการ:** API ไม่ทำงาน  
**วิธีแก้:**
```bash
cd server
npm start
```

### 2. Database ไม่มีคอลัมน์ใหม่
**อาการ:** Error: Unknown column 'purchased_from'  
**วิธีแก้:**
```bash
./check_system.sh
# หรือ
mysql -u root -p mobile_shop_db < fix_missing_fields.sql
```

### 3. ข้อมูลไม่แสดงเวลาแก้ไข
**อาการ:** ช่อง "ซื้อจาก" และ "หมวดเครื่อง" ว่าง  
**วิธีแก้:**
- Hard refresh (Ctrl+Shift+R)
- เช็คว่า JavaScript ดึงข้อมูลถูกต้อง
- รัน migration อีกครั้ง

### 4. Database name ไม่ตรงกัน
**อาการ:** ไม่พบฐานข้อมูล  
**วิธีแก้:**
- เช็คว่าใช้ `mobile_shop_db` หรือ `ilovephone_db`
- แก้ใน `.env` file:
```
DB_NAME=mobile_shop_db
```

## ✅ เกณฑ์ผ่านการทดสอบ

ระบบจะถือว่า**ผ่านการทดสอบ** ถ้า:

1. ✅ สร้างเครื่องใหม่ได้ และบันทึกลง Database
2. ✅ แก้ไขข้อมูลได้ และอัพเดทใน Database
3. ✅ ลบข้อมูลได้ และหายจาก Database
4. ✅ ฟิลด์ใหม่ (ซื้อจาก, หมวดเครื่อง) บันทึกและแสดงผลถูกต้อง
5. ✅ Filter เดือน/ปี ทำงานได้
6. ✅ Search/ค้นหา ทำงานได้
7. ✅ แสดงข้อมูลครบทุก Tab (Stock/Sold/Removed)

## 📞 สรุป

หากทดสอบแล้ว:
- **7/7 ผ่าน** = ✅ ระบบทำงานสมบูรณ์
- **5-6/7 ผ่าน** = ⚠️ มีปัญหาเล็กน้อย ตรวจสอบข้อที่ไม่ผ่าน
- **< 5/7 ผ่าน** = ❌ มีปัญหา ต้องแก้ไข

---

**เวอร์ชั่น:** 2.0  
**อัพเดท:** 31 ตุลาคม 2025  
**ระบบ:** iLovePhone Store Management System

