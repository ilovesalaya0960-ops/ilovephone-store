# 🚀 iLovePhone Store - คู่มือการใช้งานเซิร์ฟเวอร์

## 📋 สารบัญ
1. [การเริ่มต้นใช้งาน](#การเริ่มต้นใช้งาน)
2. [คำสั่งพื้นฐาน](#คำสั่งพื้นฐาน)
3. [URL สำหรับเข้าใช้งาน](#url-สำหรับเข้าใช้งาน)
4. [แก้ไขปัญหา](#แก้ไขปัญหา)

---

## 🎯 การเริ่มต้นใช้งาน

### ข้อกำหนด
- Python 3.x
- Node.js 14+
- MySQL 8.0+

### เริ่มใช้งานครั้งแรก

1. **เปิด Terminal**
2. **ไปที่โฟลเดอร์โปรเจค:**
   ```bash
   cd /Users/chakkapong/Sites/ilovephone-store
   ```

3. **เริ่มเซิร์ฟเวอร์:**
   ```bash
   ./start-servers.sh
   ```

4. **เปิดเบราว์เซอร์:**
   ```
   http://localhost:8000
   ```

---

## ⚡ คำสั่งพื้นฐาน

### 🟢 เริ่มเซิร์ฟเวอร์
```bash
./start-servers.sh
```
**ผลลัพธ์:**
- Frontend Server: http://localhost:8000 ✅
- Backend API Server: http://localhost:5001 ✅

### 🔴 หยุดเซิร์ฟเวอร์
```bash
./stop-servers.sh
```

### 🔄 Restart เซิร์ฟเวอร์
```bash
./restart-servers.sh
```

### 🔍 ตรวจสอบสถานะ
```bash
./check-servers.sh
```

---

## 🌐 URL สำหรับเข้าใช้งาน

| บริการ | URL | คำอธิบาย |
|--------|-----|----------|
| **หน้าเว็บหลัก** | http://localhost:8000 | Frontend สำหรับใช้งานทั่วไป |
| **API Backend** | http://localhost:5001 | REST API สำหรับจัดการข้อมูล |
| **API Docs** | http://localhost:5001/api | ดูรายการ API endpoints |

---

## 🔧 แก้ไขปัญหา

### ❌ เว็บไซต์เปิดไม่ขึ้น

**ทำตามลำดับ:**

1. **ตรวจสอบสถานะเซิร์ฟเวอร์:**
   ```bash
   ./check-servers.sh
   ```

2. **Restart เซิร์ฟเวอร์:**
   ```bash
   ./restart-servers.sh
   ```

3. **Hard Refresh เบราว์เซอร์:**
   - **Mac:** กด `Cmd + Shift + R`
   - **Windows/Linux:** กด `Ctrl + Shift + R`

4. **ยังไม่ได้? ดูคู่มือละเอียด:**
   ```bash
   cat TROUBLESHOOTING.md
   ```

---

## 📊 ตัวอย่างการใช้งาน

### เริ่มงานแต่ละวัน
```bash
cd /Users/chakkapong/Sites/ilovephone-store
./start-servers.sh
# เปิดเบราว์เซอร์ไปที่ http://localhost:8000
```

### ปิดงานแต่ละวัน
```bash
./stop-servers.sh
```

### ตรวจสอบก่อนใช้งาน
```bash
./check-servers.sh
```

---

## 💡 Tips & Tricks

### 1. สร้าง Alias (ทางลัด)
เพิ่มในไฟล์ `~/.zshrc` หรือ `~/.bashrc`:

```bash
# iLovePhone Store Aliases
alias ilove-start="cd /Users/chakkapong/Sites/ilovephone-store && ./start-servers.sh"
alias ilove-stop="cd /Users/chakkapong/Sites/ilovephone-store && ./stop-servers.sh"
alias ilove-restart="cd /Users/chakkapong/Sites/ilovephone-store && ./restart-servers.sh"
alias ilove-check="cd /Users/chakkapong/Sites/ilovephone-store && ./check-servers.sh"
alias ilove-logs="tail -f /tmp/backend-server.log"
```

จากนั้นรัน:
```bash
source ~/.zshrc  # หรือ source ~/.bashrc
```

### 2. Bookmark ใน Browser
บันทึก URL เหล่านี้ไว้ใน Bookmarks:
- 🏠 Frontend: http://localhost:8000
- 🔧 API: http://localhost:5001/api/equipment

### 3. ดู Logs แบบ Real-time
```bash
# Backend logs
tail -f /tmp/backend-server.log

# Frontend logs
tail -f /tmp/frontend-server.log
```

---

## 📁 โครงสร้างโปรเจค

```
ilovephone-store/
├── index.html              # หน้าเว็บหลัก
├── script.js               # JavaScript หลัก
├── style.css               # CSS หลัก
├── server/                 # Backend folder
│   ├── server.js           # Express server
│   ├── routes/             # API routes
│   ├── config/             # Database config
│   └── .env                # Environment variables
├── start-servers.sh        # ⭐ เริ่มเซิร์ฟเวอร์
├── stop-servers.sh         # ⭐ หยุดเซิร์ฟเวอร์
├── restart-servers.sh      # ⭐ Restart เซิร์ฟเวอร์
├── check-servers.sh        # ⭐ ตรวจสอบสถานะ
├── TROUBLESHOOTING.md      # คู่มือแก้ไขปัญหา
└── README_SERVERS.md       # ไฟล์นี้
```

---

## 🆘 ต้องการความช่วยเหลือ?

1. **ดูคู่มือแก้ไขปัญหา:**
   ```bash
   cat TROUBLESHOOTING.md
   ```

2. **ตรวจสอบ Logs:**
   ```bash
   tail -100 /tmp/backend-server.log
   ```

3. **ตรวจสอบสถานะทั้งหมด:**
   ```bash
   ./check-servers.sh
   ```

---

## 📝 Quick Reference

| คำสั่ง | สิ่งที่ทำ |
|--------|----------|
| `./start-servers.sh` | เริ่มเซิร์ฟเวอร์ทั้งหมด |
| `./stop-servers.sh` | หยุดเซิร์ฟเวอร์ทั้งหมด |
| `./restart-servers.sh` | Restart เซิร์ฟเวอร์ทั้งหมด |
| `./check-servers.sh` | ตรวจสอบสถานะ |
| `Cmd+Shift+R` (Mac) | Hard refresh browser |
| `Ctrl+Shift+R` (Win) | Hard refresh browser |

---

**📅 Last Updated:** 19 พ.ย. 2568  
**👨‍💻 Developer:** iLovePhone Store Team  
**📧 Support:** ดูไฟล์ TROUBLESHOOTING.md

