# อัพเดตระบบเครื่องใหม่ (New Devices System Update)

## 🆕 ฟีเจอร์ใหม่

### 1. ช่อง "ซื้อจาก" (Purchased From)
- เพิ่มช่องสำหรับระบุแหล่งที่ซื้อเครื่องมา
- สามารถกรอกข้อมูล เช่น ร้านค้า, ตัวแทน, บุคคล
- ไม่บังคับกรอก (Optional field)

### 2. ช่อง "หมวดเครื่อง" (Device Category)
- เพิ่มช่องสำหรับจัดหมวดหมู่เครื่อง
- มี 2 ตัวเลือก:
  - **No Active** (ค่าเริ่มต้น)
  - **Active**
- บังคับกรอก (Required field)

### 3. ตัวเลือก RAM เพิ่มเติม
- เพิ่ม RAM 1 GB
- เพิ่ม RAM 3 GB

## 📋 ข้อมูลทางเทคนิค

### Database Changes
```sql
-- ฟิลด์ใหม่ที่เพิ่มเข้ามาในตาราง new_devices
purchased_from VARCHAR(100)      -- แหล่งที่ซื้อมา
device_category ENUM('No Active', 'Active') DEFAULT 'No Active'  -- หมวดเครื่อง
```

### API Changes
- **POST** `/api/newDevices` - รองรับฟิลด์ `purchased_from` และ `device_category`
- **PUT** `/api/newDevices/:id` - รองรับการอัพเดตฟิลด์ใหม่

## 🚀 วิธีการติดตั้ง

### ขั้นตอนที่ 1: รัน Database Migration

เลือกวิธีใดวิธีหนึ่ง:

**วิธีที่ 1: ใช้สคริปต์อัตโนมัติ (แนะนำ)**
```bash
./apply_all_migrations.sh
```

**วิธีที่ 2: รันด้วยมือ**
```bash
# Migration 1: เพิ่มฟิลด์ purchased_from
mysql -u root -p mobile_shop_db < server/migrations/add_purchased_from_field.sql

# Migration 2: เพิ่มฟิลด์ device_category
mysql -u root -p mobile_shop_db < server/migrations/add_device_category_field.sql
```

### ขั้นตอนที่ 2: Restart Server
```bash
cd server
npm start
```

### ขั้นตอนที่ 3: ทดสอบระบบ
1. เปิดเว็บแอพพลิเคชัน
2. คลิกปุ่ม "เพิ่มเครื่องใหม่"
3. ตรวจสอบว่ามีฟิลด์ใหม่:
   - ซื้อจาก
   - หมวดเครื่อง (No Active/Active)
4. กรอกข้อมูลและบันทึก
5. ลองแก้ไขข้อมูลเพื่อตรวจสอบว่าแสดงผลถูกต้อง

## 🔧 การแก้ไขปัญหา

### ปัญหา: ช่อง "ซื้อจาก" ไม่บันทึก
**สาเหตุ**: ยังไม่ได้รัน migration
**วิธีแก้**: รัน migration ตามขั้นตอนที่ 1

### ปัญหา: ข้อผิดพลาด "Unknown column 'purchased_from'"
**สาเหตุ**: ฐานข้อมูลยังไม่มีคอลัมน์นี้
**วิธีแก้**: ตรวจสอบว่ารัน migration สำเร็จหรือไม่

### ปัญหา: ข้อผิดพลาด "Unknown column 'device_category'"
**สาเหตุ**: ฐานข้อมูลยังไม่มีคอลัมน์นี้
**วิธีแก้**: รัน migration ตัวที่ 2

## 📊 ตรวจสอบ Migration

ตรวจสอบว่า migration สำเร็จแล้วหรือไม่:

```sql
-- เข้า MySQL
mysql -u root -p mobile_shop_db

-- ตรวจสอบโครงสร้างตาราง
DESCRIBE new_devices;

-- ควรเห็นฟิลด์ใหม่:
-- purchased_from     VARCHAR(100)
-- device_category    ENUM('No Active', 'Active')
```

## 📝 หมายเหตุ

- ฟิลด์ "ซื้อจาก" เป็น Optional สามารถไม่กรอกก็ได้
- ฟิลด์ "หมวดเครื่อง" เป็น Required ต้องเลือก (ค่าเริ่มต้นคือ "No Active")
- ข้อมูลเก่าที่มีอยู่แล้วจะถูกตั้งค่า device_category เป็น "No Active" อัตโนมัติ

## 🎯 การใช้งาน

### เพิ่มเครื่องใหม่
1. คลิกปุ่ม "เพิ่มเครื่องใหม่"
2. กรอกข้อมูลตามปกติ
3. **ใหม่**: กรอก "ซื้อจาก" (ถ้ามี)
4. **ใหม่**: เลือก "หมวดเครื่อง" (No Active หรือ Active)
5. กรอกข้อมูลอื่นๆ และกดบันทึก

### แก้ไขข้อมูล
1. คลิกปุ่ม "แก้ไข" ที่เครื่องที่ต้องการ
2. ข้อมูล "ซื้อจาก" และ "หมวดเครื่อง" จะแสดงในฟอร์ม
3. แก้ไขและบันทึก

---

**สร้างเมื่อ**: 31 ตุลาคม 2025  
**เวอร์ชั่น**: 2.0  
**ผู้พัฒนา**: iLovePhone Store Management System

