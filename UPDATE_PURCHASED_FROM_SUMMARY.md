# 📋 สรุปการเพิ่มคอลัมน์ "ซื้อจาก"

## ✅ HTML - เสร็จสมบูรณ์แล้ว

### เครื่องใหม่
- ✅ แท็บ สต๊อค - เพิ่มคอลัมน์ "ซื้อจาก" (colspan: 9 → 10)
- ✅ แท็บ ขายแล้ว - เพิ่มคอลัมน์ "ซื้อจาก" (colspan: 11 → 12)
- ✅ แท็บ ตัดออก - เพิ่มคอลัมน์ "ซื้อจาก" (colspan: 11 → 12)
- ✅ แท็บ เคลม - เพิ่มคอลัมน์ "ซื้อจาก" (colspan: 10 → 11)

### เครื่องมือสอง
- ✅ แท็บ สต๊อค - เพิ่มคอลัมน์ "ซื้อจาก" (colspan: 10 → 11)
- ✅ แท็บ ขายแล้ว - เพิ่มคอลัมน์ "ซื้อจาก" (colspan: 11 → 12)
- ✅ แท็บ ตัดออก - เพิ่มคอลัมน์ "ซื้อจาก" (colspan: 12 → 13)
- ✅ แท็บ เคลม - เพิ่มคอลัมน์ "ซื้อจาก" (colspan: 11 → 12)

---

## 🔄 JavaScript - กำลังอัปเดต

ต้องแก้ไข 2 ฟังก์ชันหลัก:

### 1. `displayUsedDevices()` - เครื่องมือสอง
**ที่ตั้ง:** บรรทัด ~4354

**ต้องเพิ่ม:**
```javascript
const purchasedFrom = device.purchased_from || device.purchasedFrom || '-';
```

**ต้องแก้:**
- ✅ type='stock' - เพิ่ม `<td>${purchasedFrom}</td>` หลังราคาขาย
- ✅ type='sold' - เพิ่ม `<td>${purchasedFrom}</td>` หลังราคาขาย
- ✅ type='claimed' - เพิ่ม `<td>${purchasedFrom}</td>` หลังราคาขาย  
- ✅ type='removed' - เพิ่ม `<td>${purchasedFrom}</td>` หลังราคาขาย
- ✅ อัปเดต colspan ใน empty state

### 2. `displayDevices()` - เครื่องใหม่
**ที่ตั้ง:** บรรทัด ~15878

**ต้องเพิ่ม:**
```javascript
const purchasedFrom = device.purchased_from || device.purchasedFrom || '-';
```

**ต้องแก้:**
- ✅ type='stock' - เพิ่ม `<td>${purchasedFrom}</td>` หลังราคาขาย
- ✅ type='sold' - เพิ่ม `<td>${purchasedFrom}</td>` หลังราคาขาย
- ✅ type='claimed' - เพิ่ม `<td>${purchasedFrom}</td>` หลังราคาขาย
- ✅ type='removed' - เพิ่ม `<td>${purchasedFrom}</td>` หลังราคาขาย
- ✅ อัปเดต colspan ใน empty state

---

## 📊 ตำแหน่งคอลัมน์

### เครื่องใหม่ (ทุกแท็บ)
```
ยี่ห้อ | รุ่น | สี | IMEI | RAM/ROM | ราคาซื้อ | วันที่นำเข้า | ราคาขาย | [ซื้อจาก] | ...
```

### เครื่องมือสอง (ทุกแท็บ)
```
ยี่ห้อ | รุ่น | สี | IMEI | RAM/ROM | สภาพ | ราคารับซื้อ | ราคาขาย | [ซื้อจาก] | ...
```

---

## 🎯 ฟิลด์ใน Database

**ชื่อฟิลด์:** `purchased_from` (มีอยู่แล้วใน database)

**ตัวอย่างค่า:**
- "ร้านซาลายา"
- "ร้านคลองหยง"
- "ลูกค้าเดินเข้า"
- "-" (ถ้าไม่มีข้อมูล)

---

## ✅ สรุป

### เสร็จแล้ว
- ✅ HTML ทุกแท็บ (8 ตาราง)
- ✅ อัปเดต colspan

### กำลังทำ
- 🔄 JavaScript `displayUsedDevices()`
- 🔄 JavaScript `displayDevices()`

### ทดสอบ
- ⏳ Refresh browser → ดูคอลัมน์ "ซื้อจาก"
- ⏳ ตรวจสอบข้อมูลแสดงถูกต้อง
