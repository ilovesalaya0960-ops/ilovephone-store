# 🔍 ปรับปรุงฟังก์ชันกรอง/ค้นหา - ครอบคลุมทุกคอลัมน์

## ✅ สรุปการเปลี่ยนแปลง

ปรับปรุงฟังก์ชันการค้นหาให้สามารถกรองข้อมูลได้**ทุกคอลัมน์**ในทุกหมวด

---

## 📊 หมวดที่อัปเดตแล้ว

### 1. ✅ เครื่องใหม่ (New Devices)
**ฟังก์ชัน:** `applyNewDevicesFilter()`

**แท็บที่รองรับ:** สต๊อค, ขายแล้ว, ตัดออก, เคลม (4 แท็บ)

**คอลัมน์ที่ค้นหาได้:**
- ✅ ยี่ห้อ (brand)
- ✅ รุ่น (model)
- ✅ สี (color)
- ✅ IMEI
- ✅ RAM/ROM
- ✅ ราคาซื้อ (purchase_price)
- ✅ ราคาขาย (sale_price)
- ✅ วันที่นำเข้า/วันที่ขาย/วันที่เคลม (import_date, sale_date, claim_date)
- ✅ หมายเหตุ (note)
- ✅ ซื้อจาก (purchased_from)
- ✅ กำไร (คำนวณจาก sale_price - purchase_price)

**ตัวอย่างการใช้งาน:**
```javascript
// ค้นหา "4990" จะเจอ:
- ราคาขาย ฿4,990
- IMEI ที่มี 4990
- RAM/ROM 4/128 GB (ถ้าพิมพ์ "4")
```

---

### 2. ✅ เครื่องมือสอง (Used Devices)
**ฟังก์ชัน:** `applyUsedDevicesFilter()`

**แท็บที่รองรับ:** สต๊อค, ขายแล้ว, ตัดออก, เคลม (4 แท็บ)

**คอลัมน์ที่ค้นหาได้:**
- ✅ ยี่ห้อ (brand)
- ✅ รุ่น (model)
- ✅ สี (color)
- ✅ IMEI
- ✅ RAM/ROM
- ✅ สภาพเครื่อง (device_condition)
- ✅ ราคารับซื้อ (purchase_price)
- ✅ ราคาขาย (sale_price)
- ✅ วันที่รับซื้อ/วันที่ขาย/วันที่เคลม (purchase_date, sale_date, claim_date)
- ✅ หมายเหตุ (note)
- ✅ ซื้อจาก (purchased_from)
- ✅ กำไร (คำนวณจาก sale_price - purchase_price)

**ตัวอย่างการใช้งาน:**
```javascript
// ค้นหา "excellent" จะเจอ:
- เครื่องที่มีสภาพ "ดีมาก" (excellent)
```

---

### 3. ✅ เครื่องผ่อน (Installments)
**ฟังก์ชัน:** `filterInstallments()`

**แท็บที่รองรับ:** กำลังผ่อน, ผ่อนสำเร็จ, ยึดเครื่อง (3 แท็บ)

**คอลัมน์ที่ค้นหาได้:**
- ✅ ยี่ห้อ (brand)
- ✅ รุ่น (model)
- ✅ สี (color)
- ✅ IMEI
- ✅ RAM/ROM
- ✅ ชื่อลูกค้า (customerName)
- ✅ เบอร์โทร (customerPhone)
- ✅ เงินดาวน์ (downPayment)
- ✅ ค่างวด (installmentAmount)
- ✅ จำนวนงวด (totalInstallments)
- ✅ งวดที่จ่ายแล้ว (paidInstallments)
- ✅ หมายเหตุ (note)
- ✅ ราคาเครื่อง (devicePrice)
- ✅ วันที่ (completedDate, seizedDate)

**ตัวอย่างการใช้งาน:**
```javascript
// ค้นหา "081234" จะเจอ:
- เบอร์โทรลูกค้า 0812345678

// ค้นหา "1000" จะเจอ:
- เงินดาวน์ 1,000
- ค่างวด 1,000
- ราคาเครื่อง 10,000 (มี "1000" ในตัว)
```

---

## 🔧 การทำงานของฟังก์ชัน

### ตัวอย่าง Code (เครื่องใหม่ - แท็บสต๊อค)

```javascript
if (searchTerm) {
    stockDevices = stockDevices.filter(device => {
        const purchasePrice = (device.purchase_price || '').toString();
        const salePrice = (device.sale_price || '').toString();
        const importDate = formatDate(device.import_date || '');
        const note = (device.note || '').toLowerCase();
        const purchasedFrom = (device.purchased_from || '').toLowerCase();
        
        return device.brand.toLowerCase().includes(searchTerm) ||
               device.model.toLowerCase().includes(searchTerm) ||
               device.color.toLowerCase().includes(searchTerm) ||
               device.imei.toLowerCase().includes(searchTerm) ||
               (device.ram + '/' + device.rom).includes(searchTerm) ||
               purchasePrice.includes(searchTerm) ||
               salePrice.includes(searchTerm) ||
               importDate.includes(searchTerm) ||
               note.includes(searchTerm) ||
               purchasedFrom.includes(searchTerm);
    });
}
```

---

## 🎯 คุณสมบัติที่เพิ่มเข้ามา

### 1. ค้นหาตัวเลข
- ✅ ราคา: `4990` → เจอ ฿4,990
- ✅ IMEI: `116538` → เจอ IMEI 116538
- ✅ RAM/ROM: `4` → เจอ 4/128 GB

### 2. ค้นหาวันที่
- ✅ `23/12/2025` → เจอวันที่นำเข้า/ขาย
- ✅ `12/2025` → เจอวันที่ในเดือน 12/2025
- ✅ `2025` → เจอวันที่ในปี 2025

### 3. ค้นหาข้อความ
- ✅ `oppo` → เจอยี่ห้อ Oppo
- ✅ `a6x` → เจอรุ่น A6x
- ✅ `ม่วง` → เจอสี ม่วง
- ✅ `excellent` → เจอสภาพ ดีมาก
- ✅ `ร้านซาลายา` → เจอซื้อจาก "ร้านซาลายา"

### 4. ค้นหาหมายเหตุ
- ✅ ค้นหาคำใดๆ ในหมายเหตุได้

### 5. ค้นหาชื่อ-เบอร์ลูกค้า (เครื่องผ่อน)
- ✅ `สมชาย` → เจอชื่อลูกค้า
- ✅ `0812345678` → เจอเบอร์โทร

---

## 💡 ตัวอย่างการใช้งาน

### Case 1: ค้นหาเครื่อง Oppo A6x ที่ยังอยู่ในสต๊อค
```
พิมพ์: "oppo a6x"
ผลลัพธ์: เจอเครื่อง Oppo A6x ทั้งหมดในแท็บสต๊อค
```

### Case 2: ค้นหาเครื่องราคา 4,990 บาท
```
พิมพ์: "4990"
ผลลัพธ์: เจอเครื่องที่ราคาขาย 4,990 บาท
```

### Case 3: ค้นหาเครื่องที่ซื้อมาจากร้านซาลายา
```
พิมพ์: "ซาลายา"
ผลลัพธ์: เจอเครื่องที่ซื้อจาก "ร้านซาลายา"
```

### Case 4: ค้นหาลูกค้าชื่อสมชาย
```
พิมพ์: "สมชาย" (ในหน้าเครื่องผ่อน)
ผลลัพธ์: เจอรายการผ่อนของลูกค้าชื่อสมชาย
```

### Case 5: ค้นหา IMEI
```
พิมพ์: "116538"
ผลลัพธ์: เจอเครื่องที่มี IMEI 116538
```

---

## 🔍 หมวดอื่นๆ

### ขายฝาก (Pawn)
- ⏳ ยังไม่มีฟังก์ชันค้นหาแยก (ใช้ระบบกรองวันที่)

### เครื่องซ่อม (Repairs)
- ⏳ ยังไม่มีฟังก์ชันค้นหาแยก (ใช้ระบบกรองวันที่)

### อุปกรณ์ (Accessories)
- ⏳ ยังไม่มีฟังก์ชันค้นหาแยก (อาจจะไม่จำเป็น เพราะมีข้อมูลน้อย)

### ซิมการ์ด (SIM Cards)
- ⏳ ยังไม่มีฟังก์ชันค้นหา

---

## ✅ สรุป

**หมวดที่ปรับปรุงแล้ว:** 3 หมวด
1. ✅ เครื่องใหม่ (4 แท็บ)
2. ✅ เครื่องมือสอง (4 แท็บ)
3. ✅ เครื่องผ่อน (3 แท็บ)

**จำนวนคอลัมน์ที่ค้นหาได้เพิ่มขึ้น:**
- เครื่องใหม่: 5 คอลัมน์ → **11 คอลัมน์** (+6)
- เครื่องมือสอง: 6 คอลัมน์ → **13 คอลัมน์** (+7)
- เครื่องผ่อน: 5 คอลัมน์ → **14 คอลัมน์** (+9)

**คุณสมบัติใหม่:**
- ✅ ค้นหาราคา (ทศนิยม/ไม่มีทศนิยม)
- ✅ ค้นหาวันที่ (รองรับหลายรูปแบบ)
- ✅ ค้นหาหมายเหตุ
- ✅ ค้นหา "ซื้อจาก"
- ✅ ค้นหากำไร (คำนวณ)
- ✅ ค้นหาข้อมูลลูกค้า (ชื่อ, เบอร์)

---

## 🚀 การใช้งาน

1. **Refresh browser:** `Cmd + Shift + R`
2. **ไปที่หน้าใดก็ได้:** เครื่องใหม่, เครื่องมือสอง, หรือเครื่องผ่อน
3. **พิมพ์ในช่องค้นหา:** พิมพ์อะไรก็ได้ที่ต้องการหา
4. **ผลลัพธ์:** แสดงทันที (รองรับทุกคอลัมน์!)

---

**สถานะ:** ✅ พร้อมใช้งาน

**ไฟล์ที่แก้ไข:**
- `/Users/chakkapong/Sites/ilovephone-store/script.js`
  - ฟังก์ชัน `applyNewDevicesFilter()`
  - ฟังก์ชัน `applyUsedDevicesFilter()`
  - ฟังก์ชัน `filterInstallments()`

**จำนวนบรรทัดที่แก้:** ~150 บรรทัด
**Linter Errors:** 0 ❌ ไม่มี error

**ปรับปรุงล่าสุด:** 1 ธันวาคม 2568
