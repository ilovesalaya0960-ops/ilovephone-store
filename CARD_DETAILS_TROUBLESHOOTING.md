# การแก้ปัญหาการแสดงรายละเอียดการ์ด

## ปัญหาที่พบ

เมื่อกดที่การ์ดในหน้าแดชบอร์ด แสดง "ไม่มีข้อมูลในขณะนี้" ทั้งที่มีข้อมูลอยู่จริง

## สาเหตุที่เป็นไปได้

### 1. ข้อมูลไม่ตรงกับเงื่อนไขการกรอง

#### การ์ดเครื่องใหม่/เครื่องมือสอง
**เงื่อนไข:**
- ต้องมี `status = 'stock'`
- ต้องเป็นร้านที่เลือก

**วิธีตรวจสอบ:**
1. เปิด Console (F12)
2. กดที่การ์ดเครื่องใหม่หรือเครื่องมือสอง
3. ดู Console log:
```javascript
Getting card details: { cardType: "new-devices", store: "klongyong", ... }
New devices from API: 5 devices
Filtered (stock only): 0 devices  // ← ถ้าเป็น 0 แสดงว่าไม่มีเครื่องในสต็อก
```

**วิธีแก้:**
- ตรวจสอบว่าข้อมูลในฐานข้อมูลมี `status = 'stock'` หรือไม่
- ถ้าข้อมูลทั้งหมดเป็น `status = 'sold'` จะไม่แสดง
- เพิ่มเครื่องใหม่ลงสต็อก

#### การ์ดขายฝาก
**เงื่อนไข:**
- ต้องมี `status = 'active'` (ยังไม่รับคืน, ยังไม่ยึด)
- ต้องเป็นร้านที่เลือก

**วิธีตรวจสอบ:**
1. เปิด Console (F12)
2. กดที่การ์ดขายฝาก
3. ดู Console log:
```javascript
Getting card details: { cardType: "pawn", store: "klongyong", ... }
Pawn devices from API: 2 items
Filtered (active only): 1 items  // ← จำนวนที่แสดง
```

**ตัวอย่าง:**
- ร้านคลองโยงมี 2 รายการ
- 1 รายการ status = 'active' → แสดง ✅
- 1 รายการ status = 'returned' → ไม่แสดง ❌

**วิธีแก้:**
- ตรวจสอบสถานะในฐานข้อมูล
- ถ้ารับเครื่องคืนแล้วจะไม่แสดง (เป็นปกติ)
- ถ้าต้องการดูทั้งหมด ไปที่เมนู "ขายฝาก" แทน

#### การ์ดซ่อม/ผ่อน
**เงื่อนไข:**
- ต้องมี `status = 'completed'`
- ต้องมี `completedDate` 
- `completedDate` ต้องอยู่ในเดือนที่เลือก
- ต้องเป็นร้านที่เลือก

**วิธีตรวจสอบ:**
```javascript
Getting card details: { cardType: "repair", month: "2025-10", ... }
// ถ้าเดือนปัจจุบันคือ ตุลาคม 2025
// แต่รายการซ่อมเสร็จเดือน กันยายน 2025
// → ไม่แสดง
```

**วิธีแก้:**
- เปลี่ยนเดือนที่เลือกในแดชบอร์ด
- เลือกเดือนที่มีรายการเสร็จสิ้น

### 2. ร้านไม่ตรงกัน

**ปัญหา:**
- เลือกร้าน "ศาลายา"
- แต่ข้อมูลอยู่ที่ร้าน "คลองโยง"

**วิธีแก้:**
- เปลี่ยนร้านที่ dropdown ด้านบน
- หรือตรวจสอบว่าข้อมูลอยู่ร้านไหน

### 3. ฐานข้อมูลว่างเปล่า

**ปัญหา:**
- ยังไม่มีข้อมูลในฐานข้อมูลเลย

**วิธีแก้:**
- เพิ่มข้อมูลทดสอบ
- หรือตรวจสอบว่าโหลดข้อมูลจาก API สำเร็จหรือไม่

## วิธีการ Debug

### ขั้นตอนที่ 1: เปิด Console
1. กด `F12` หรือ Right Click → Inspect
2. ไปที่แท็บ `Console`

### ขั้นตอนที่ 2: กดที่การ์ด
1. คลิกที่การ์ดที่ต้องการดู
2. สังเกต Console log

### ขั้นตอนที่ 3: วิเคราะห์ผลลัพธ์

#### ตัวอย่างที่ 1: มีข้อมูล (ปกติ)
```javascript
Getting card details: {
  cardType: "pawn",
  store: "klongyong",
  month: "2025-10"
}
Pawn devices from API: 2 items
Filtered (active only): 1 items
Card details (pawn): {
  totalItems: 1,
  currentStore: "klongyong",
  currentMonth: "2025-10"
}
// → แสดงตารางพร้อมข้อมูล 1 รายการ
```

#### ตัวอย่างที่ 2: ไม่มีข้อมูล (กรองหมด)
```javascript
Getting card details: {
  cardType: "pawn",
  store: "salaya",  // ← เลือกร้านศาลายา
  month: "2025-10"
}
Pawn devices from API: 2 items  // ← มี 2 รายการ
Filtered (active only): 0 items // ← แต่ไม่ใช่ร้านศาลายา หรือไม่ active
Card details (pawn): {
  totalItems: 0,  // ← ไม่มีข้อมูลแสดง
  currentStore: "salaya",
  currentMonth: "2025-10"
}
// → แสดงข้อความ "ไม่มีข้อมูลในขณะนี้"
```

#### ตัวอย่างที่ 3: API ล้มเหลว
```javascript
Error loading card details: Network Error
// → แสดงข้อความ error สีแดง
```

## การแก้ปัญหาตามกรณี

### กรณีที่ 1: มีข้อมูลแต่ไม่แสดง

**เช็คลิสต์:**
- [ ] ร้านที่เลือกถูกต้องหรือไม่
- [ ] เดือนที่เลือกถูกต้องหรือไม่
- [ ] สถานะข้อมูลถูกต้องหรือไม่
- [ ] Console มี error หรือไม่

**ตัวอย่างการแก้:**
```sql
-- ตรวจสอบข้อมูลในฐานข้อมูล
SELECT id, brand, model, status, store, receive_date 
FROM pawn_devices 
WHERE store = 'klongyong';

-- ผลลัพธ์:
-- PWN123 | Apple | iPhone 14 | returned | klongyong | 2025-10-21
-- PWN124 | Samsung | S24 | active | klongyong | 2025-10-22

-- → รายการแรก status = 'returned' จะไม่แสดงในการ์ด
-- → รายการที่สอง status = 'active' จะแสดง ✅
```

### กรณีที่ 2: ข้อมูลซ่อม/ผ่อนไม่แสดง

**สาเหตุ:** เดือนไม่ตรง

**วิธีแก้:**
1. ตรวจสอบ `completedDate` ของรายการ
2. เปลี่ยนเดือนในแดชบอร์ดให้ตรงกับเดือนที่เสร็จสิ้น

**ตัวอย่าง:**
```javascript
// รายการซ่อมเสร็จวันที่ 15 กันยายน 2025
completedDate: "2025-09-15"

// แดชบอร์ดเลือกเดือน ตุลาคม 2025
currentMonth: "2025-10"

// → ไม่แสดง (เดือนไม่ตรง)

// แก้ไข: เปลี่ยนเดือนเป็น กันยายน 2025
```

### กรณีที่ 3: ไม่มีข้อมูลเลย

**สาเหตุ:** ฐานข้อมูลว่าง หรือยังไม่มีข้อมูลร้านนี้

**วิธีแก้:**
1. เพิ่มข้อมูลทดสอบ
2. หรือเปลี่ยนไปดูร้านอื่น

## ข้อความแจ้งเตือนที่ปรับปรุงใหม่

เมื่อไม่มีข้อมูล จะแสดง:

```
📭 ไม่มีข้อมูลในขณะนี้

กรุณาตรวจสอบ:
✓ เลือกร้านที่ถูกต้อง
✓ มีเครื่องขายฝากที่ยังไม่รับคืนหรือไม่
```

ข้อความจะแตกต่างกันตามประเภทการ์ด:
- **เครื่องใหม่/มือสอง**: ตรวจสอบว่ามีเครื่องในสต็อก
- **ซ่อม/ผ่อน**: ตรวจสอบเดือนที่มีรายการเสร็จสิ้น
- **ขายฝาก**: ตรวจสอบว่ามีเครื่องที่ยังไม่รับคืน

## ตัวอย่างสถานการณ์จริง

### สถานการณ์ที่ 1: ร้านคลองโยง - การ์ดขายฝาก

**ข้อมูลในฐานข้อมูล:**
- รายการที่ 1: status = 'active' (ยังไม่รับคืน) ✅
- รายการที่ 2: status = 'returned' (รับคืนแล้ว) ❌

**ผลลัพธ์:**
- การ์ดแสดง: 1 เครื่อง
- คลิกที่การ์ด → แสดง 1 รายการ (รายการที่ 1)
- รายการที่ 2 จะไม่แสดงเพราะรับคืนแล้ว

### สถานการณ์ที่ 2: ร้านศาลายา - การ์ดซ่อม

**ข้อมูลในฐานข้อมูล:**
- ซ่อมเสร็จเดือน กันยายน: 3 รายการ
- ซ่อมเสร็จเดือน ตุลาคม: 0 รายการ

**ผลลัพธ์:**
- แดshบอร์ดเลือกเดือน ตุลาคม
- การ์ดแสดง: 0 รายการ
- คลิกที่การ์ด → "ไม่มีข้อมูลในขณะนี้"

**วิธีแก้:**
- เปลี่ยนเดือนเป็น กันยายน → จะแสดง 3 รายการ

## Console Log Reference

### Log ที่ควรเห็นเมื่อมีข้อมูล

```javascript
// 1. เริ่มโหลด
Getting card details: {
  cardType: "pawn",
  store: "klongyong",
  month: "2025-10",
  year: "2025",
  monthNum: "10"
}

// 2. ดึงข้อมูลจาก API
Pawn devices from API: 2 items

// 3. กรองตามเงื่อนไข
Filtered (active only): 1 items

// 4. แสดงผล
Card details (pawn): {
  totalItems: 1,
  currentStore: "klongyong",
  currentMonth: "2025-10"
}
```

### Log ที่ควรเห็นเมื่อไม่มีข้อมูล

```javascript
// 1. เริ่มโหลด
Getting card details: { ... }

// 2. ดึงข้อมูลจาก API
Pawn devices from API: 2 items

// 3. กรองตามเงื่อนไข
Filtered (active only): 0 items  // ← กรองหมด

// 4. แสดงข้อความไม่มีข้อมูล
Card details (pawn): {
  totalItems: 0,  // ← ไม่มีข้อมูล
  ...
}
```

## สรุป

การแสดง "ไม่มีข้อมูลในขณะนี้" **ไม่ได้หมายความว่าระบบ error** แต่หมายความว่า:
- ✅ ระบบทำงานถูกต้อง
- ✅ ดึงข้อมูลจาก API สำเร็จ
- ✅ แต่ข้อมูลที่ดึงมาไม่ตรงกับเงื่อนไขการกรอง

**ขั้นตอนการแก้ปัญหา:**
1. เปิด Console (F12)
2. กดที่การ์ด
3. ดู log จำนวนข้อมูล
4. ตรวจสอบเงื่อนไข (ร้าน, เดือน, สถานะ)
5. ปรับเงื่อนไขให้ตรงกับข้อมูล

**ถ้ายังมีปัญหา:**
- แคปหน้าจอ Console log
- ส่งมาให้ดู
- หรือ export ข้อมูลจากฐานข้อมูลมาดู

