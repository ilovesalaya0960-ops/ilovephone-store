# ปรับปรุง UI ฟอร์มเพิ่มรายการผ่อน Partner

## การเปลี่ยนแปลง

ปรับ UI ฟอร์มให้เป็น **2 คอลัมน์** เพื่อความสวยงามและใช้พื้นที่อย่างมีประสิทธิภาพ

## ก่อนแก้ไข ❌

**1 คอลัมน์ยาวลงมา:**
```
ยี่ห้อ      [_________]
รุ่น       [_________]
สี         [_________]
IMEI       [_________]
RAM        [_________]
ROM        [_________]
ชื่อลูกค้า  [_________]
เบอร์โทร    [_________]
```

## หลังแก้ไข ✅

**2 คอลัมน์สวยงาม:**
```
ยี่ห้อ    [_____]  |  รุ่น      [_____]
สี       [_____]  |  IMEI      [_____]
RAM      [_____]  |  ROM       [_____]
ชื่อลูกค้า [_____]  |  เบอร์โทร   [_____]
```

## โครงสร้างฟอร์มใหม่

### 📱 ข้อมูลเครื่อง (3 แถว × 2 คอลัมน์)

**แถวที่ 1:**
- ยี่ห้อ (ซ้าย)
- รุ่น (ขวา)

**แถวที่ 2:**
- สี (ซ้าย)
- IMEI (ขวา)

**แถวที่ 3:**
- RAM (ซ้าย)
- ROM (ขวา)

### 👤 ข้อมูลลูกค้า (1 แถว × 2 คอลัมน์)

**แถวที่ 1:**
- ชื่อลูกค้า (ซ้าย)
- เบอร์โทรศัพท์ (ขวา)

### 💰 ข้อมูลการผ่อน (4 แถว × 2 คอลัมน์)

**แถวที่ 1:**
- ราคาทุน (ซ้าย)
- ยอดจัด (ขวา)

**แถวที่ 2:**
- เงินดาวน์ (ซ้าย)
- ค่าคอม (ขวา) - แสดงเฉพาะ Partner

**แถวที่ 3:**
- งวดที่ผ่อน (ซ้าย)
- ยอดผ่อน/งวด (ขวา)

**แถวที่ 4:**
- วันวางเงินดาวน์ (ซ้าย)
- วันครบกำหนดจ่ายค่างวด (ขวา)

**แถวที่ 5:**
- หมายเหตุ (full width)

## โครงสร้าง HTML

ใช้ `form-row` class สำหรับแต่ละแถว:

```html
<!-- แถวที่ 1: ยี่ห้อ, รุ่น -->
<div class="form-row">
    <div class="form-group">
        <label for="installmentBrand">ยี่ห้อ <span class="required">*</span></label>
        <input type="text" id="installmentBrand" name="brand" required>
    </div>
    <div class="form-group">
        <label for="installmentModel">รุ่น <span class="required">*</span></label>
        <input type="text" id="installmentModel" name="model" required>
    </div>
</div>
```

## CSS ที่ใช้

```css
.form-row {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 1rem;
}

.form-group {
    display: flex;
    flex-direction: column;
}

/* Responsive: mobile จะกลับเป็น 1 column */
@media (max-width: 768px) {
    .form-row {
        grid-template-columns: 1fr;
    }
}
```

## ประโยชน์

✅ **ประหยัดพื้นที่** - ลดความยาวของฟอร์มลง 50%
✅ **ดูสวยงาม** - จัดเรียงเป็นระเบียบมากขึ้น
✅ **ง่ายต่อการกรอก** - เห็นข้อมูลได้ครบในหน้าจอ
✅ **Responsive** - บนมือถือจะกลับเป็น 1 คอลัมน์อัตโนมัติ
✅ **เข้าถึงง่าย** - Scroll น้อยลง เห็นข้อมูลทั้งหมดได้เร็วขึ้น

## การทดสอบ

1. **Desktop/Laptop:**
   - เปิดฟอร์มเพิ่มรายการผ่อน
   - ตรวจสอบว่าฟิลด์จัดเป็น 2 คอลัมน์

2. **Tablet:**
   - ลอง resize หน้าจอ
   - ตรวจสอบว่า layout ยังดูดี

3. **Mobile:**
   - เปิดบนมือถือ
   - ตรวจสอบว่ากลับเป็น 1 คอลัมน์อัตโนมัติ

## ไฟล์ที่แก้ไข

- ✅ `index.html` - ปรับโครงสร้าง HTML ให้ใช้ form-row

## หมายเหตุ

- CSS ของ `.form-row` มีอยู่แล้วใน `style.css`
- ใช้ `grid` แทน `flexbox` เพื่อความยืดหยุ่น
- รองรับ responsive design อัตโนมัติ
- ไม่กระทบกับฟอร์มอื่นๆ ในระบบ

