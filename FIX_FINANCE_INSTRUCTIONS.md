# 🔧 แก้ไขปัญหา Finance ไม่บันทึก

## ⚠️ สาเหตุ
ตาราง `installment_devices` ยังขาดคอลัมน์ที่จำเป็น:
- ❌ `commission` (ค่าคอม)
- ❌ `next_payment_due_date` (วันครบกำหนด)
- ❌ `installment_type` (ประเภทการผ่อน)
- ❌ `finance` (ข้อมูล finance) ← **สำคัญ!**
- ❌ `store` (ร้าน)

---

## ✅ วิธีแก้ไข (เลือกวิธีใดวิธีหนึ่ง)

### 🎯 วิธีที่ 1: ใช้ Sequel Ace (แนะนำ!)

1. เปิด **Sequel Ace**
2. เลือกฐานข้อมูล **`ilovephone_store`**
3. กดแท็บ **Query**
4. เปิดไฟล์ **`SIMPLE_FIX_FINANCE.sql`**
5. Copy ทั้งหมด → Paste ลงใน Query
6. กด **Run** (หรือ Cmd+R)
7. ดูผลลัพธ์ว่า "Migration completed!" ✅

---

### 🎯 วิธีที่ 2: ใช้ Terminal

```bash
cd /Users/chakkapong/Sites/ilovephone-store
./fix_installment_table.sh
```

หรือ

```bash
mysql -u root ilovephone_store < SIMPLE_FIX_FINANCE.sql
```

---

## 🔍 ตรวจสอบว่าสำเร็จ

ใน Sequel Ace → Query:

```sql
DESCRIBE installment_devices;
```

ต้องเห็นคอลัมน์เหล่านี้:
- ✅ `commission`
- ✅ `next_payment_due_date`
- ✅ `installment_type`
- ✅ `finance` ← **ต้องมี!**
- ✅ `store`

---

## 🔄 หลังรัน Migration

1. **Refresh หน้าเว็บ** (F5)
2. ลองแก้ไขรายการผ่อนอีกครั้ง
3. กรอก **finance** และ **ยอดจัด**
4. กด **บันทึก**
5. กดแก้ไขอีกครั้งเพื่อดูว่าข้อมูลบันทึกแล้ว ✅

---

## 📝 ตรวจสอบ Console

เปิด DevTools (F12) → Console → ดู log:

```
💾 Finance value: finn connext
💾 Sale price value: 6990
✅ API PUT response: ...
```

---

## ❌ ถ้ายังไม่ได้

ส่ง screenshot ของ:
1. ผลลัพธ์จาก `DESCRIBE installment_devices;`
2. Console error (ถ้ามี)
3. Network tab → ดู request/response ของ PUT `/installments/:id`

---

## 📞 ติดปัญหา?

ให้ส่ง:
- Screenshot จาก Sequel Ace
- Console log
- Error message (ถ้ามี)

